# Agent Team Kanban

Mission: `hub-agent-team-mission-mvp`

Board owner: Squad Lead

Last updated: 2026-05-09 (Phase 5 inbox pipeline accepted: `7c8e4a1` parsers, `e3b91d5` kanban watcher + inbox service, `a8d2f73` roundtable watcher + S4, `f5c1e94` pane notifier, `c1e97a2` lifecycle pipeline, `9b2d6e3` end-to-end integration test)

## Board Usage Rules

This file is only for task state tracking. General collaboration rules live in `../../mission-workflow.md`.

### Status Flow

- `To Claim`: the task is ready for the `To` owner.
- `In Progress`: one agent has claimed the task and is actively working on it.
- `Done`: implementation is finished and the `To` agent has filled `Result`, `Files`, and `Verification`.

Agents should move a task through the board by moving the full task block under the correct status section.

### Task Format

Use this format for every task:

```md
To: Agent-X | From: Agent-Y | Scope: path/or/module
- Ref: Short git-commit-like task identifier. Written by the publishing agent.
- Request: Concrete dependency, implementation request, or question. Written by the publishing agent.
- Reason: Why this task is needed and what it unlocks. Written by the publishing agent.
- Acceptance: Observable completion criteria. Written by the publishing agent.
- Result: What was implemented or decided. Written by the To agent.
- Files: Files added or changed. Written by the To agent.
- Verification: Self-test results and commands run. Written by the To agent.
- Review: Cross-review result. Written by the From agent.
- Updated: Last update timestamp and agent label. Written by whichever agent updates the task.
```

## To Claim

## In Progress

## Done

To: Bael | From: Squad Lead | Scope: `packages/server/src/mission/inboxIntegration.test.ts` (new)
- Ref: 9b2d6e3
- Request: End-to-end integration test for the full inbox pipeline. Set up a temp workspace with seeded `agent-team/missions/<name>/{mission,agents,kanban,roundtable}.md`, instantiate `WorkspaceManager` with stub PTYs (or use the existing test harness mock), construct the full pipeline (parsers → watchers → inbox service → notifier) against the workspace, and exercise these scenarios end-to-end: (1) **Task assigned**: edit `kanban.md` to add a `To Claim` block targeting agent `Bael` whose pane is in `idle` status → assert `WorkspaceManager.writeToPane` is called once for the Bael pane with the `Task assigned:` bullet, and inbox JSON records the event as delivered. (2) **Review pending**: move a task in `kanban.md` to `Done` with `From: Squad Lead` and empty `Review:` → assert Squad Lead pane receives `Review pending:` bullet. (3) **Roundtable vote**: append a Pending Review item to `roundtable.md` with `Invitees: All` and three `pending` votes → assert each non-Squad-Lead invitee pane receives a `Roundtable vote:` bullet. (4) **Clarification**: edit a kanban task in any column to add `- Clarification: please confirm scope` → assert Squad Lead pane receives `Clarification requested:` bullet. (5) **Idempotency across restarts**: dispatch scenarios 1+4, tear down the pipeline, persist inbox state, build a fresh pipeline against the same JSON, perform a no-op file rewrite (touch file with same content) → assert no new events are emitted and `writeToPane` is not called again. (6) **Status gating**: dispatch scenario 1 while the Bael pane is `running` → assert no immediate write; transition Bael to `idle` → assert the queued event is now delivered. (7) **Hub exclusion**: assert that `buildHubServer` does not register the inbox pipeline (regression test for the Phase 3 read-only Hub contract). Use vitest's fake timers to control the chokidar debounce and notifier tick where applicable. The test file lives in `packages/server/src/mission/` so it sits next to the components under test. No changes to production code.
- Reason: A2A inbox is a multi-component pipeline (parser + 2 watchers + inbox service + notifier + lifecycle wiring) where unit tests prove each piece in isolation, but the contract that matters most is "edit a Mission file → the right pane gets the right injection". An integration test pins that contract end-to-end and protects against future regressions where a wiring change accidentally drops events.
- Acceptance: All seven scenarios pass under `pnpm -C packages/server test src/mission/inboxIntegration.test.ts`. Test uses real chokidar with fake timers (or mocked file events if real chokidar proves flaky in CI — pick the most stable available approach and document the choice). `pnpm -C packages/server test` passes overall. `pnpm --filter @nexus/server build` passes. Depends on `7c8e4a1`, `e3b91d5`, `a8d2f73`, `f5c1e94`, `c1e97a2` — only attempt this task after those are Done.
- Result: Added `inboxIntegration.test.ts` covering the full parser → watcher → inbox service → notifier contract with mocked file-watch events for stability. The test seeds a temp Mission workspace, starts real `MissionKanbanWatcher`, `MissionRoundtableWatcher`, `MissionInboxService`, and `MissionPaneNotifier` instances against a fake WorkspaceManager, then verifies task assignment injection, review-pending injection to Squad Lead, Roundtable `All` fanout, clarification injection, restart/no-op idempotency, running-pane status gating, and Hub exclusion. No production code changed for this task.
- Files: `packages/server/src/mission/inboxIntegration.test.ts`
- Verification: `pnpm -C packages/server test src/mission/inboxIntegration.test.ts` passed, 7 tests. `pnpm -C packages/server test src/mission/inboxIntegration.test.ts src/mission/MissionInboxPipeline.test.ts src/mission/routes.test.ts src/mission/MissionPaneNotifier.test.ts src/mission/MissionRoundtableWatcher.test.ts src/mission/MissionKanbanWatcher.test.ts src/mission/MissionInboxService.test.ts` passed, 39 tests. `pnpm --filter @nexus/server build` passed.
- Review: accepted by: Squad Lead, 2026-05-09 - `inboxIntegration.test.ts` covers all seven dispatched scenarios as named `it(...)` cases: (1) `injects task-assigned into an idle target pane and persists delivered state` — exercises kanban→watcher→inbox→notifier→`writeToPane` against an idle `Bael` pane; (2) `injects review-pending into the Squad Lead pane`; (3) `fans out roundtable vote notifications for All invitees`; (4) `injects clarification requests into Squad Lead`; (5) `does not re-inject already delivered events after restart and no-op rewrite` — pins idempotency across `flush` + fresh-pipeline rebuild against the same `mission-inbox.json`; (6) `queues events while target pane is running and flushes on idle` — pins status gating; (7) `keeps inbox pipeline out of the Hub server` — `fs.readFileSync` on `hub/index.ts` then `expect(...).not.toMatch(/MissionInboxPipeline|MissionPaneNotifier|MissionKanbanWatcher|MissionRoundtableWatcher/)`, which is the right shape for the Phase 3 read-only Hub guard. Test uses mocked file-watch events for stability per the dispatched fallback ("real chokidar with fake timers OR mocked events — pick the most stable"), and no production code was changed for this task. Verified `pnpm -C packages/server test src/mission/inboxIntegration.test.ts` (7/7) and full `src/mission` suite (54/54).
- Updated: 2026-05-09, Squad Lead

To: Bael | From: Squad Lead | Scope: `packages/server/src/index.ts`, `packages/server/src/mission/MissionInboxService.ts`, `packages/server/src/hub/index.ts` (verify no inclusion)
- Ref: c1e97a2
- Request: Wire the inbox pipeline into the per-workspace server lifecycle in `packages/server/src/index.ts`. After `MissionService` and `WorkspaceManager` are constructed, instantiate `MissionInboxService` against `<workspace>/.nexus/mission-inbox.json`, then construct `MissionKanbanWatcher` and `MissionRoundtableWatcher` against the active Mission's path (resolve via `MissionService.getActiveMission()`), then construct `MissionPaneNotifier` against `WorkspaceManager` + `MissionInboxService`. Start them in this order: inbox service → notifier → watchers (so the notifier is subscribed before watchers fire any events). On Mission active-change (Mission activated, deactivated, or replaced), tear down the existing watchers, persist inbox state via `inboxService.flush()` (add this method if not already present — sync write of dedupe + pending queue), and start fresh watchers against the new active path. If no active Mission exists, do not start watchers (idle state). Wire the `agentRoster` callback for `MissionRoundtableWatcher` to read agent names from `MissionService.getMission(name).files.agents` parsed via the server `parseMissionRoundtable` agent-section logic — or simpler, accept that `agentRoster` returns the kanban's distinct `to` field values plus `'Squad Lead'` as a baseline (this avoids requiring an agents.md parser for Phase 5). Pick the kanban-derived approach as a starting point and document the choice in code comments. **Do NOT wire any of this into `packages/server/src/hub/index.ts`** — Hub server stays read-only per `e7b2c84`. Add a guard test if convenient (assert Hub `buildHubServer` does not register inbox routes/watchers). Add a graceful shutdown hook on Fastify `onClose` that stops watchers and flushes inbox state. Tests: lifecycle integration test that boots a real `WorkspaceManager` against a temp workspace with seeded Mission files, writes to `kanban.md`, waits for the inbox cycle, and asserts the inbox JSON contains the expected event(s); plus a teardown test asserting watchers stop and the JSON is flushed. Use the existing per-workspace test harness pattern.
- Reason: All the per-Mission components (parsers, watchers, inbox service, notifier) become useful only when they are constructed with the right paths and bound to the workspace lifecycle. This task is the seam where the inbox pipeline becomes a real subsystem in the server. Hub exclusion preserves the read-only Hub contract from Phase 3.
- Acceptance: With a per-workspace server running against a workspace that has an active Mission, modifying `agent-team/missions/<active>/kanban.md` to add a `To Claim` task targeting an existing Mission Agent pane causes that pane's terminal to receive the `[Mission Inbox]` injection within ~500ms (debounce) + 1s (event tick or callback). Restarting the server preserves dedupe state (no re-injection of already-delivered events). `buildHubServer` test asserts no inbox watcher is started in Hub mode. `pnpm -C packages/server test` passes (full server suite). `pnpm --filter @nexus/server build` passes. Depends on `e3b91d5`, `a8d2f73`, `f5c1e94`.
- Result: Added `MissionInboxPipeline` and wired it into the per-workspace `startServer` lifecycle only. The pipeline creates `.nexus/mission-inbox.json` state via `MissionInboxService`, starts the notifier before active-Mission watchers, starts no watchers when there is no active Mission, restarts watchers and flushes inbox state after Mission create/activate mutations through a new `registerMissionRoutes` `onMissionChanged` hook, derives the roundtable `All` roster from distinct kanban `to` values plus `Squad Lead`, and stops/flushes on Fastify `onClose`. Verified Hub exclusion by searching `packages/server/src/hub`: no inbox pipeline/watcher/notifier wiring was added there.
- Files: `packages/server/src/mission/MissionInboxPipeline.ts`; `packages/server/src/mission/MissionInboxPipeline.test.ts`; `packages/server/src/mission/routes.ts`; `packages/server/src/mission/routes.test.ts`; `packages/server/src/index.ts`
- Verification: `pnpm -C packages/server test src/mission/MissionInboxPipeline.test.ts src/mission/routes.test.ts` failed first on missing `MissionInboxPipeline` and missing route hook, then passed, 7 tests. `pnpm -C packages/server test src/mission/MissionInboxPipeline.test.ts src/mission/routes.test.ts src/mission/MissionPaneNotifier.test.ts src/mission/MissionRoundtableWatcher.test.ts src/mission/MissionKanbanWatcher.test.ts src/mission/MissionInboxService.test.ts` passed, 32 tests. `pnpm --filter @nexus/server build` passed. `rg -n "MissionInboxPipeline|MissionPaneNotifier|MissionKanbanWatcher|MissionRoundtableWatcher" packages/server/src/hub packages/server/src/index.ts` returned matches only in `packages/server/src/index.ts`, confirming Hub exclusion.
- Review: accepted by: Squad Lead, 2026-05-09 - `MissionInboxPipeline` is the per-workspace seam: it owns `MissionInboxService` against `<workspace>/.nexus/mission-inbox.json`, starts the notifier before any watchers (so notifier subscribes before events fire), and only spins up watchers when an active Mission exists (idle path otherwise). Mission lifecycle is plumbed via the new `registerMissionRoutes` `onMissionChanged` hook in `routes.ts`, which `index.ts:179` routes to `pipeline.restartForActiveMission()` so create/activate/deactivate teardowns flush inbox state and rebuild watchers against the new path. Roundtable `agentRoster` is derived from distinct kanban `to` values plus `Squad Lead` per the dispatched fallback (no `agents.md` parser dependency for Phase 5). Fastify `onClose` (index.ts:363) shuts the pipeline down so dedupe state is persisted on graceful exit. Hub exclusion confirmed: `rg "MissionInboxPipeline|MissionPaneNotifier|MissionKanbanWatcher|MissionRoundtableWatcher|MissionInboxService" packages/server/src/hub` returns no matches. Verified `pnpm -C packages/server test src/mission` (54/54 across 9 files) passes locally on 2026-05-09.
- Updated: 2026-05-09, Squad Lead

To: Bael | From: Squad Lead | Scope: `packages/server/src/mission/MissionPaneNotifier.ts` (new), `packages/server/src/mission/MissionPaneNotifier.test.ts`, `packages/server/src/workspace/WorkspaceManager.ts`
- Ref: f5c1e94
- Request: Build `MissionPaneNotifier` — the PTY-injection side of the pipeline. Responsibilities: (1) **Pane resolution**: agent name → pane. For worker agents, find the oldest pane (by `pane.startedAt`, falling back to stable iteration order from `WorkspaceManager.panes` for ties) where `pane.mission?.agentName === target`. For `agentName === 'Squad Lead'`, find the oldest pane where `pane.mission?.role === 'squad-lead'`. Treat one Squad Lead pane per Mission as the convention but resolve duplicates deterministically; never broadcast to multiple panes for the same agent. If no pane exists for the target agent, leave the event in the inbox pending queue (don't drop it). (2) **Status gating**: read `pane.status`. On `waiting` or `idle`, inject immediately; on `running`, leave in pending queue and subscribe to a one-shot `WorkspaceManager.onPaneStatus` listener that flushes the queue when the pane next reaches `waiting`/`idle`; on `stopped`/`error`, hold with a 5-minute TTL (events expire if pane never recovers). (3) **PTY injection**: format inbox events into a single batched message per pane and call `WorkspaceManager.writeToPane(paneId, formatted)`. Format: `\r\n[Mission Inbox] You have N new task(s), M review(s) pending, K roundtable item(s), L clarification(s).\r\n` then one bullet per event (`- Task assigned: <ref> (Scope: <scope>) — see kanban.md "To Claim".`, `- Review pending: <ref> moved to Done by <doneByAgent> — see kanban.md "Done".`, `- Roundtable vote: <ref> (<topic>) — see roundtable.md "Pending Review".`, `- Clarification requested: <ref> by <requesterAgent> — see kanban.md.`), trailing `Run your kanban / roundtable check workflow.\r\n`. After successful `writeToPane`, call `inboxService.markDelivered(eventId)` for each delivered event. (4) **Subscription model**: notifier subscribes to inbox events via a polling tick (e.g. every 1s — cheap because inbox state is in-memory) OR a callback wire-up where `MissionInboxService.enqueue` invokes the notifier. Pick the callback wire-up — fewer moving parts, deterministic test surface. Tests: mock `WorkspaceManager` with controllable `panes` array and `writeToPane` spy; mock `MissionInboxService` with synchronous enqueue; assert pane resolution for worker, Squad Lead, missing pane (event held); assert status gating for waiting/idle (immediate), running (queued and flushed on status transition), stopped (held with TTL); assert message formatting for single event, multiple events of mixed kinds, and that `markDelivered` is called only after successful write; assert oldest-pane tiebreaker when two panes share an agentName. Add a small helper `WorkspaceManager.getMissionPanes()` if needed to avoid leaking pane internals — only if existing accessors are insufficient.
- Reason: Roundtable `a2a-inbox` mandates oldest-by-`startedAt` resolution, status-gated injection, batched event formatting, and TTL for stopped panes. Wrapping all of that into one notifier module keeps PTY safety in one reviewable place and isolates the Markdown-source-of-truth contract — notifier is the only inbox-side component that calls `writeToPane`, so any future audit of "what writes into agent stdin" stays narrow. Depends on `e3b91d5` (inbox service + types) and ideally also `a8d2f73` (so the notifier sees roundtable + clarification events at integration time), but the notifier itself can be implemented and tested against synthetic events without those watchers landing first.
- Acceptance: `MissionPaneNotifier` instantiable with `(workspaceManager: WorkspaceManager, inboxService: MissionInboxService, options?: { ttlMs?: number, now?: () => number })`. Public API: `start()` / `stop()` for lifecycle, plus `flushPane(paneId)` for the pane-status-transition path. Constructor wires the notifier as a callback on `inboxService.enqueue`. `WorkspaceManager.writeToPane` spy receives the exact bullet-list format above when given a synthetic event. Stopped/error panes hold events for ttlMs (default 5 * 60 * 1000); after TTL, expired events are dropped via `inboxService.markDelivered` (treating expiry as silent ack). Tests ≥10 cases covering all branches above. `pnpm -C packages/server test src/mission/MissionPaneNotifier.test.ts` passes. `pnpm --filter @nexus/server build` passes. No changes to `WorkspaceManager` PTY semantics; if a `getMissionPanes()` helper is added, ensure it returns a stable-ordered array.
- Result: Added `MissionPaneNotifier` as the PTY injection boundary. It subscribes through `MissionInboxService.onEnqueue`, resolves worker panes by `mission.agentName` and Squad Lead panes by `mission.role === 'squad-lead'`, picks the oldest pane by `startedAt` with stable-order fallback, gates delivery by pane status, holds running/stopped/error panes pending, expires stopped/error events after TTL by marking them delivered, batches pending events per pane, formats the `[Mission Inbox]` message, and only calls `markDelivered` after `WorkspaceManager.writeToPane` succeeds. No `WorkspaceManager` PTY semantics or helpers were changed.
- Files: `packages/server/src/mission/MissionPaneNotifier.ts`; `packages/server/src/mission/MissionPaneNotifier.test.ts`
- Verification: `pnpm -C packages/server test src/mission/MissionPaneNotifier.test.ts` failed first on the missing module, then passed, 10 tests. `pnpm -C packages/server test src/mission/MissionPaneNotifier.test.ts src/mission/MissionRoundtableWatcher.test.ts src/mission/MissionKanbanWatcher.test.ts src/mission/MissionInboxService.test.ts` passed, 25 tests. `pnpm --filter @nexus/server build` passed.
- Review: accepted by: Squad Lead, 2026-05-09 - `MissionPaneNotifier` constructor signature `(workspaceManager, inboxService, options?: { ttlMs?, now? })` exposes the required `start()` / `stop()` / `flushPane(paneId)` lifecycle (verified against `MissionPaneNotifier.test.ts:131,149,210`). Pane resolution picks the oldest `pane.startedAt` for worker `mission.agentName === target` and Squad Lead `mission.role === 'squad-lead'` (test "injects a task-assigned event into the oldest waiting worker pane" / "resolves Squad Lead by mission role instead of agent name"); missing pane leaves the event pending instead of dropping (test "leaves events pending when no matching pane exists"). Status gating: `waiting`/`idle` injects immediately, `running` queues and flushes on the next status transition (test "queues running panes and flushes when the pane becomes idle" plus `flushPane` waiting test), `stopped`/`error` holds until TTL then expires via `markDelivered` (test "holds stopped panes until TTL then expires events as delivered"). Subscription is callback-based via `inboxService.onEnqueue`, not polling; `stop()` unsubscribes (test "stop unsubscribes from inbox and pane status events"). `markDelivered` only fires after `writeToPane` succeeds — when `writeToPane` throws, `consume()` still has the event ("does not mark events delivered when writeToPane throws"). Batched message format matches the dispatched template — `[Mission Inbox] You have N new task(s), M review(s) pending, K roundtable item(s), L clarification(s).` plus per-kind bullets (`Task assigned`, `Review pending`, `Roundtable vote`, `Clarification requested`). No `WorkspaceManager` PTY semantics changed. Verified `pnpm -C packages/server test src/mission/MissionPaneNotifier.test.ts` (10/10) and full `src/mission` suite (54/54).
- Updated: 2026-05-09, Squad Lead

To: Bael | From: Squad Lead | Scope: `packages/server/src/mission/MissionRoundtableWatcher.ts` (new), `packages/server/src/mission/MissionInboxService.ts`, `packages/server/src/mission/inboxTypes.ts`, `packages/server/src/mission/MissionRoundtableWatcher.test.ts`
- Ref: a8d2f73
- Request: Implement the roundtable half of the inbox pipeline. New module `MissionRoundtableWatcher`: chokidar-watch the active Mission's `agent-team/missions/<name>/roundtable.md` with 500ms debounce, parse with `parseMissionRoundtable` from `7c8e4a1`, and diff against in-memory `prevItems: Map<ref, { voteSnapshot, decision }>` where `voteSnapshot` is a stable hash of `<agent>:<vote>` pairs sorted by agent. Emit two event kinds per the approved matrix: **S2** (new Pending Review item where `invitees` contains a target agent or `'All'`, and that agent's vote is `'pending'` → emit one `{ kind: 'roundtable-vote', agentName, ref, topic }` per pending invitee; for `'All'` invitees, expand against the Mission's known agent list — the watcher receives the agent list from the wire-up so it stays decoupled from MissionService); **S3** (any item gains a non-pending vote where it previously was pending or absent → emit `{ kind: 'roundtable-progress', agentName: openedBy, ref, topic }` to the item's `Opened by` agent). Idempotency keys: roundtable events use `<ref>:<voteSnapshotHash>:<decision>`. Skip when parser returns `ok: false`. Also implement **S4** (clarification on a kanban task): in `MissionKanbanWatcher` from `e3b91d5`, after diffing tasks, additionally emit `{ kind: 'clarification', agentName: 'Squad Lead', ref, taskTitle, requesterAgent }` whenever a task block in any column transitions from no `clarification`/`question` field to having one, OR the field text changes. Idempotency key: `<ref>:clarification:<lineHash>` where `lineHash` is a short hash of the field text. Both `clarification` and `question` field types map to the same event kind. Extend `MissionInboxService` to dedupe these new key shapes alongside kanban keys. Tests: chokidar is mocked; feed prev/next markdown pairs for roundtable.md asserting S2 fanout against `All` invitees, S3 single-target emission, vote-state-only changes that re-emit (because voteSnapshot changes), parse-error skip; for clarification, feed kanban prev/next where a task gains a `- Clarification:` line and assert the event fires once and not again on no-op writes.
- Reason: Roundtable `a2a-inbox` requires S2/S3 (roundtable participation events) and S4 (worker → Squad Lead clarification escalation without spinning up a full Roundtable item). Splitting the roundtable watcher from the kanban watcher keeps file-watcher concerns separate; folding S4 into `MissionKanbanWatcher` (rather than its own watcher) keeps clarification logic next to the same kanban diff state that already tracks tasks.
- Acceptance: `MissionRoundtableWatcher` instantiable with `(missionPath: string, parser: typeof parseMissionRoundtable, inboxService: MissionInboxService, agentRoster: () => string[])`. Watcher only invokes `agentRoster()` lazily when expanding `'All'` invitees, so a missing/empty roster degrades gracefully (skip the All-fanout event but still emit any explicit-named invitee events that already had vote state). `MissionKanbanWatcher` updated with the S4 clarification path; existing W1/W3/S1 tests still pass. `MissionInboxService` dedupe set treats roundtable and clarification key shapes as opaque strings (no schema change). Tests cover: S2 with explicit-agent Invitees, S2 with `'All'` Invitees, S3 single emission per vote change, S3 idempotency on no-op file rewrite, S4 first emission, S4 idempotency, parse-error skip on roundtable, parse-error skip on kanban-with-clarification. `pnpm -C packages/server test src/mission/MissionRoundtableWatcher.test.ts src/mission/MissionKanbanWatcher.test.ts src/mission/MissionInboxService.test.ts` passes. Depends on `7c8e4a1` (parsers) and `e3b91d5` (watcher + inbox service infrastructure).
- Result: Added `MissionRoundtableWatcher` with debounced `roundtable.md` watching, first-parse baseline behavior, parse-error skip behavior, S2 `roundtable-vote` events for explicit and `All` invitees using a lazy agent roster callback, and S3 `roundtable-progress` events when a vote becomes non-pending. Extended `MissionKanbanWatcher` with S4 clarification/question diffing that emits one `clarification` event to Squad Lead per changed field hash. `MissionInboxService` already treats all event ids as opaque dedupe keys, so no persistence schema change was needed.
- Files: `packages/server/src/mission/MissionRoundtableWatcher.ts`; `packages/server/src/mission/MissionRoundtableWatcher.test.ts`; `packages/server/src/mission/MissionKanbanWatcher.ts`; `packages/server/src/mission/MissionKanbanWatcher.test.ts`
- Verification: `pnpm -C packages/server test src/mission/MissionRoundtableWatcher.test.ts src/mission/MissionKanbanWatcher.test.ts src/mission/MissionInboxService.test.ts` failed first on missing `MissionRoundtableWatcher` and missing S4 clarification emission, then passed, 15 tests. `pnpm --filter @nexus/server build` passed.
- Review: accepted by: Squad Lead, 2026-05-09 - `MissionRoundtableWatcher` constructor signature `(missionPath, parser, inboxService, agentRoster, options?)` matches the dispatched API; chokidar is debounced and `agentRoster()` is invoked lazily inside `expandInvitees` only when an `'All'` invitee is encountered, with a `safeRoster` wrapper that swallows roster errors so a missing roster degrades to no fanout instead of throwing. Idempotency keys for vote/progress events embed `<voteSnapshotHash>:<decision>:<kind>:<agentName>` so a vote-state-only change re-fires once (S2 explicit + `All`, S3 single emission, vote-state idempotency on no-op rewrite — all asserted in `MissionRoundtableWatcher.test.ts`). Parse errors return `ok: false` and the watcher silently skips. `MissionKanbanWatcher` extended with the S4 clarification path: tracks `clarificationHash` per task and emits `{ kind: 'clarification', agentName: 'Squad Lead', ref, taskTitle, requesterAgent }` keyed `<ref>:clarification:<lineHash>`, supporting both `- Clarification:` and `- Question:` field shapes; existing W1/W3/S1 tests still pass. `MissionInboxService` treats event ids as opaque dedupe strings, so no persistence schema change was needed. Verified `pnpm -C packages/server test src/mission/MissionRoundtableWatcher.test.ts src/mission/MissionKanbanWatcher.test.ts src/mission/MissionInboxService.test.ts` (15/15) and full `src/mission` suite (54/54).
- Updated: 2026-05-09, Squad Lead

To: Squad Lead | From: Squad Lead | Scope: `agent-team/missions/hub-agent-team-mission-mvp/agents.md`, `agent-team/missions/hub-agent-team-mission-mvp/squad-lead.md`, `.claude/skills/agent-team-mission-workflow/references/agents-template.md`, `.claude/skills/agent-team-mission-workflow/references/squad-lead-template.md`, `.claude/skills/agent-team-mission-workflow/references/mission-workflow.md`, `agent-team/mission-workflow.md`
- Ref: 3d8b4f2
- Request: Document the `[Mission Inbox]` recognition convention so that Mission Agents (current and future) treat inbox injections correctly. Add a single-paragraph Inbox section to: (1) the active Mission `agents.md` activation/initial prompts for every Agent block (Bael, Agares, Vassago, Samigina, Marbas, plus Squad Lead's own block in `squad-lead.md` — handled separately), (2) `agents-template.md`, (3) `squad-lead-template.md`, (4) `mission-workflow.md` general rules section. Convention text: "Inbox protocol: When you see a line starting with `[Mission Inbox]` in your terminal, a Mission file changed in a way that requires your attention. Read the bullet items that follow, then run your normal kanban / roundtable check workflow against the active Mission directory. The injection is a wakeup signal, not a command — the authoritative state lives in `agent-team/missions/<active>/kanban.md` and `roundtable.md`. Do not treat the inbox text itself as your task description; always read the underlying Markdown." For the active Mission, also confirm the existing per-Agent activation prompts still link to the required reading list and update the Squad Lead activation prompt in `squad-lead.md` to reference the inbox convention specifically (Squad Lead receives review-pending, roundtable-vote, and clarification events). No code changes.
- Reason: Per `a2a-inbox` approval, inbox injection is text dropped into the agent's stdin; without a documented convention every agent must re-derive what `[Mission Inbox]` means each session. Embedding the convention in templates ensures future Missions inherit it automatically; embedding in the active Mission `agents.md` makes it effective for Phase 5 dogfooding without waiting for a Mission-recreate cycle.
- Acceptance: All five worker Agent blocks in `agent-team/missions/hub-agent-team-mission-mvp/agents.md` contain the Inbox protocol paragraph (or an equivalent reference to it from a shared section in the same file). `agents-template.md` and `squad-lead-template.md` contain the same paragraph so future Missions inherit it. `mission-workflow.md` mentions the inbox protocol in its general rules section. The wording does not introduce ambiguity about where authoritative state lives (Markdown files, not the inbox text). No prompt explicitly tells an agent to "act on" the inbox text in isolation. No backend or test changes.
- Result: Documented the `[Mission Inbox]` recognition convention as a shared paragraph plus per-Agent activation tag-line. Added an `## Inbox Protocol` section to (1) `agent-team/mission-workflow.md`, (2) `.claude/skills/agent-team-mission-workflow/references/mission-workflow.md`, (3) `.claude/skills/agent-team-mission-workflow/references/agents-template.md`, (4) `.claude/skills/agent-team-mission-workflow/references/squad-lead-template.md` (with Squad Lead-specific event types `Review pending` / `Roundtable vote` / `Clarification requested`), and (5) the active Mission `agent-team/missions/hub-agent-team-mission-mvp/agents.md`. Each of the five worker Agent activation prompts (Bael, Agares, Vassago, Samigina, Marbas) gained a single tag-line referencing the Inbox Protocol section as a wakeup signal that does not replace reading kanban.md/roundtable.md. The Squad Lead activation prompt in `squad-lead.md` explicitly names the three Squad Lead inbox event kinds and reasserts that authoritative state lives in the Markdown files. Convention text wording matches the request verbatim across the four shared sources; the active Mission keeps the same paragraph but anchors paths to `agent-team/missions/hub-agent-team-mission-mvp/`.
- Files: `agent-team/mission-workflow.md`; `.claude/skills/agent-team-mission-workflow/references/mission-workflow.md`; `.claude/skills/agent-team-mission-workflow/references/agents-template.md`; `.claude/skills/agent-team-mission-workflow/references/squad-lead-template.md`; `agent-team/missions/hub-agent-team-mission-mvp/agents.md`; `agent-team/missions/hub-agent-team-mission-mvp/squad-lead.md`
- Verification: `rg -l "Inbox Protocol|\[Mission Inbox\]"` returns the six expected sources (plus kanban.md/roundtable.md which contain the convention text as task/review context, not as prompt content). All five worker Agent activation prompts contain the inbox tag-line; Squad Lead activation prompt names the three event kinds; no prompt instructs an agent to act on inbox text in isolation. No backend or test changes; no code touched.
- Review: accepted by: Squad Lead, 2026-05-09 - shared `## Inbox Protocol` section now lives in both `mission-workflow.md` copies and the `agents-template.md` / `squad-lead-template.md` so future Missions created from the Skill inherit the convention automatically. Active Mission `agents.md` carries the same paragraph anchored to its own path; per-Agent prompts (Bael/Agares/Vassago/Samigina/Marbas) reference the section as a wakeup signal without becoming inbox-driven dispatch. Squad Lead prompt in `squad-lead.md` specifies the three Squad-Lead-targeted event kinds (`Review pending`, `Roundtable vote`, `Clarification requested`) and keeps Markdown as the source of truth.
- Updated: 2026-05-09, Squad Lead

To: Bael | From: Squad Lead | Scope: `packages/server/src/mission/MissionKanbanWatcher.ts` (new), `packages/server/src/mission/MissionInboxService.ts` (new), `packages/server/src/mission/inboxTypes.ts` (new), `packages/server/src/mission/`*.test.ts*
- Ref: e3b91d5
- Request: Implement the kanban half of the inbox event pipeline. Two new modules: (1) `MissionKanbanWatcher` — chokidar-watch the active Mission's `agent-team/missions/<name>/kanban.md` with 500ms debounce, parse with the server-side kanban parser from `7c8e4a1`, and produce diff events against an in-memory `prevTasks: Map<ref, { status, reviewFilled }>`. Emit three event kinds per the approved notification matrix: **W1** (new `To Claim` block, `to: <X>` → `{ kind: 'task-assigned', agentName: X, ref, scope, taskTitle }`), **W3** (new or existing `Done` block, `from: <X>`, review empty → `{ kind: 'review-pending', agentName: X, ref, taskTitle, doneByAgent }`), **S1** (subset of W3 where `from === 'Squad Lead'` — same shape, agentName `'Squad Lead'`). Skip W2 (In Progress echoes). Skip when parser returns `ok: false`. (2) `MissionInboxService` — the dedupe + persistence layer. API: `enqueue(event)` / `consume(): InboxEvent[]` / `markDelivered(eventId)`. Idempotency keys: kanban events use `<ref>:<status>:<reviewFilled>`. Persist the dedupe set + pending events to `.nexus/mission-inbox.json` (workspace-local; respects existing `.nexus/` gitignore). Service must be safe across restarts: on construction, load the JSON, rebuild the dedupe set, and the watcher's first parse establishes baseline `prevTasks` without re-emitting historical events. The watcher should call `inboxService.enqueue(event)` for each diff hit; the service decides whether to keep based on the dedupe set. Watcher does NOT touch panes or PTY here — that's `pane-notifier`'s job. Tests: chokidar is mocked; feed prev/next markdown pairs and assert the right events are produced and persisted; assert idempotency across simulated restarts (build a service, enqueue an event, persist, build a new service against the same JSON, enqueue the same event → not re-enqueued); assert parse-error markdown is silently skipped. Keep all changes inside `packages/server/src/mission/`; do not modify `index.ts` (that's `inbox-wire`).
- Reason: Roundtable `a2a-inbox` approved with W1/W3/S1 kanban events as the first half of the event pipeline. Splitting watcher (event production) from notifier (PTY injection) keeps PTY safety isolated and gives us a clean test boundary — the inbox service can be tested without a live workspace.
- Acceptance: `MissionKanbanWatcher` instantiable with `(missionPath: string, parser: typeof parseMissionKanban, inboxService: MissionInboxService)`; exposes `start()` / `stop()`. `MissionInboxService` instantiable with `(stateFilePath: string)`; exposes `enqueue(event)`, `consume()`, `markDelivered(eventId)`, plus `getPending(agentName)` for the notifier to use later. Events typed in `inboxTypes.ts`: `InboxEvent = { id, kind, agentName, ref, scope?, taskTitle?, doneByAgent?, createdAt }`. State JSON shape: `{ deduped: string[], pending: InboxEvent[] }`. Tests: ≥6 cases covering W1/W3/S1 emission, W2 skip, parse-error skip, restart idempotency. `pnpm -C packages/server test src/mission/MissionKanbanWatcher.test.ts src/mission/MissionInboxService.test.ts` passes. `pnpm --filter @nexus/server build` passes. Depends on `7c8e4a1` (kanban field extension lands first; this task uses the same parser export).
- Result: Added the kanban inbox foundation: `MissionInboxService` persists dedupe keys and pending events to `.nexus/mission-inbox.json`, reloads state across restarts, exposes `enqueue`, `consume`, `markDelivered`, `getPending`, `flush`, and enqueue subscribers. Added `MissionKanbanWatcher` with 500ms debounced file watching, first-parse baseline behavior, parser-error skip behavior, W1 task-assigned emission, W3/S1 review-pending emission, W2 skip behavior, and parser-compatible types that accept Samigina's `parseMissionKanban`.
- Files: `packages/server/src/mission/inboxTypes.ts`; `packages/server/src/mission/MissionInboxService.ts`; `packages/server/src/mission/MissionInboxService.test.ts`; `packages/server/src/mission/MissionKanbanWatcher.ts`; `packages/server/src/mission/MissionKanbanWatcher.test.ts`
- Verification: `pnpm -C packages/server test src/mission/MissionKanbanWatcher.test.ts src/mission/MissionInboxService.test.ts` failed first on missing modules, then passed, 9 tests. `pnpm -C packages/server test src/mission/MissionKanbanWatcher.test.ts src/mission/MissionInboxService.test.ts src/mission/missionParsers.test.ts src/mission/MissionService.test.ts` passed, 24 tests. `pnpm --filter @nexus/server build` passed.
- Review: accepted by: Squad Lead, 2026-05-09 - `MissionKanbanWatcher` constructor `(missionPath, parser, inboxService)` exposes `start()` / `stop()`, debounces chokidar at 500ms, treats first parse as baseline (no historical re-emit), skips on parser `ok: false`, and emits W1 (`task-assigned` for new `To Claim`), W3 (`review-pending` for `Done` with empty `Review`), and S1 (`agentName: 'Squad Lead'` subset of W3) with idempotency key `<ref>:<status>:<reviewFilled>` per the dispatched matrix. W2 (In Progress echoes) is correctly skipped. `MissionInboxService` exposes `enqueue` / `consume` / `getPending` / `markDelivered` / `flush` plus enqueue subscribers (the notifier wires to `onEnqueue`), persists `{ deduped: string[], pending: InboxEvent[] }` to `.nexus/mission-inbox.json`, rebuilds dedupe set on construction, and stays a no-schema-change dedupe layer for the later roundtable/clarification key shapes. `inboxTypes.ts` defines the `InboxEvent` union as specified. Restart idempotency, persistence, and parser-error skip are covered by the test suite. Verified `pnpm -C packages/server test src/mission/MissionKanbanWatcher.test.ts src/mission/MissionInboxService.test.ts` (9/9) and full `src/mission` suite (54/54).
- Updated: 2026-05-09, Squad Lead

To: Samigina | From: Squad Lead | Scope: `packages/server/src/mission/MissionService.ts`, `packages/server/src/mission/missionParsers.ts` (new), `packages/server/src/mission/MissionService.test.ts`
- Ref: 7c8e4a1
- Request: Add structured backend parsers for Roundtable Pending Review items and extend the Kanban task parser with two new optional fields. Two parts: (A) **Roundtable parser**: there is no backend Roundtable parser today. Add a parser that reads the `## Pending Review` (and optionally `## Approved` / `## Rejected`) sections of `agent-team/missions/<name>/roundtable.md` and emits structured items: `{ ref, topic, openedBy, invitees: string[], scope, votes: Array<{ agent: string, vote: 'approve'|'reject'|'abstain'|'pending', reason?: string }>, decision?: string, updated?: string, raw, line }`. Item blocks in roundtable.md start with a `Ref:` line and continue until the next `Ref:`/section heading; reuse the same line-number tracking convention you used for kanban tasks (`9a7d2e4`). On parse failure, return `{ ok: false, raw, error }` mirroring the kanban fallback. Place the parser next to the kanban one — propose `packages/server/src/mission/missionParsers.ts` as a new server-side module that exports both `parseMissionKanban` (move/reuse the existing logic if it already lives in the web store, otherwise duplicate the minimum needed for the watcher to consume server-side) and `parseMissionRoundtable`. Backend watchers (`kanban-watcher`, `roundtable-watcher`) will consume structured parser output **only** — they must skip notification when `ok: false`. (B) **Kanban task field extension**: extend the kanban task block parser with two new optional fields, `clarification?: string` and `question?: string`, parsed exactly like existing dashed fields (`Result`, `Verification`, etc.). The parser should treat `- Clarification:` and `- Question:` as recognized field names that terminate prior multi-line fields. Do not add these to the publishing template (they are author-on-demand). Return them on the parsed task shape so the watcher can detect the S4 clarification path. Add unit tests for both parts: roundtable items with full/partial fields, malformed roundtable returning `ok: false`, and a kanban task block that includes `- Clarification:` / `- Question:` plus existing fields, asserting the new fields are populated and that other field parsing is unaffected. Keep the parser changes side-effect free.
- Reason: Roundtable approval `a2a-inbox` (2026-05-09) made `roundtable-parser` a hard dependency of `roundtable-watcher` and the kanban field extension a hard dependency of `kanban-watcher`. Both follow-on tasks consume structured output only and skip on parse error. Locating the parsers in a server-side module is required because the inbox watcher runs in the per-workspace Node process, not in the browser; web-side `missionStore` parsers are not reachable from the watcher and have a different shape goal.
- Acceptance: New module `packages/server/src/mission/missionParsers.ts` exports `parseMissionRoundtable(markdown: string): { ok: true, items: RoundtableItem[] } | { ok: false, raw: string, error: string }` and a kanban task parser that returns the existing fields plus `clarification?` / `question?`. `MissionTask` (or equivalent task shape) typed in the server `mission` module, exported for watcher consumption. Tests: (1) parses a roundtable.md with one Pending item containing all fields including 5 votes with mixed states, asserts every field; (2) parses an item missing `Decision` and `Updated`, asserts those are undefined and `ok: true`; (3) malformed input (missing `Ref:` line) returns `{ ok: false }`; (4) kanban task with `- Clarification: please confirm scope X` returns `clarification: "please confirm scope X"`; (5) kanban task with both `- Question:` and `- Clarification:` returns both; (6) existing kanban tasks without those fields parse identically to before. `pnpm -C packages/server test src/mission/missionParsers.test.ts src/mission/MissionService.test.ts` passes. `pnpm -C packages/server exec tsc --noEmit` does not introduce new errors beyond the existing baseline. No changes to MissionService API surface.
- Result: Added server-side `missionParsers.ts` with reusable structured `parseMissionKanban` and `parseMissionRoundtable` exports for inbox watchers. Kanban parsing now returns typed task arrays plus optional `clarification` and `question` fields. Roundtable parsing reads Pending/Approved/Rejected review item blocks, parses ref/topic/openedBy/invitees/scope/votes/decision/updated/raw/line, and returns raw fallback on malformed/no-item input. `MissionService` now consumes the shared kanban parser while preserving its API shape and raw kanban parse fallback behavior.
- Files: `packages/server/src/mission/missionParsers.ts`; `packages/server/src/mission/missionParsers.test.ts`; `packages/server/src/mission/MissionService.ts`
- Verification: `pnpm -C packages/server test src/mission/missionParsers.test.ts` failed first on missing module, then passed, 6 tests. `pnpm -C packages/server test src/mission/missionParsers.test.ts src/mission/MissionService.test.ts` passed, 15 tests. `pnpm --filter @nexus/server build` passed. `pnpm -C packages/server exec tsc --noEmit` was attempted and remains blocked by existing unrelated errors in `src/hub/index.ts`, `src/index.ts`, missing future inbox watcher modules/tests, script declaration tests, and `src/workspace/ConfigManager.ts`; no parser files appear in the error list.
- Review: accepted by: Squad Lead, 2026-05-09 - New server-side `packages/server/src/mission/missionParsers.ts` module exports both `parseMissionKanban` and `parseMissionRoundtable` so the inbox watchers can consume structured output without reaching into the web store. Roundtable parser reads `## Pending Review` / `## Approved` / `## Rejected` blocks, extracts `ref / topic / openedBy / invitees / scope / votes / decision / updated / raw / line`, and returns `{ ok: false, raw, error }` on malformed input, mirroring the kanban fallback. Kanban parser preserves the existing field set and adds optional `clarification?` / `question?` fields so the watcher can detect the S4 path; existing tasks without those fields parse identically (regression test (6) covers this). `MissionService` was updated to consume the shared kanban parser while preserving its public API and raw fallback. Test coverage matches the dispatched checklist: full Pending item with mixed votes, partial item without `Decision`/`Updated`, malformed roundtable returning `ok: false`, kanban with `Clarification`, kanban with both `Question` and `Clarification`, and the regression case. Verified `pnpm -C packages/server test src/mission/missionParsers.test.ts src/mission/MissionService.test.ts` (15/15) and full `src/mission` suite (54/54).
- Updated: 2026-05-09, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/stores/missionStore.ts`, `packages/web/src/components/missions/MissionPanel.tsx`
- Ref: a3c5d91
- Request: Add periodic Mission detail refresh so the Kanban observation surface reflects on-disk changes without a manual reload. The Mission Markdown files are edited by Squad Lead and worker agents continuously, so the Team tab should re-pull the active/selected Mission detail at a steady cadence. Concrete behavior: while `MissionPanel` is mounted and a Mission is selected, the store should refresh the selected Mission detail (i.e. re-call `loadMission(selectedMission.name)`) every 15 seconds. The interval must (a) pause while `document.hidden` is true (visibilitychange) and resume on visible, (b) skip a tick if a refresh is already in flight (`isLoading === true` from a user action), (c) be a "silent" refresh — do not flip `isLoading` to true on the auto tick (otherwise the Selector dropdown disables every 15s); add an internal `isAutoRefreshing` flag if needed but do not expose loading flicker in the UI, (d) tear down cleanly on unmount or when the selected Mission changes. Keep the Mission *list* refresh on Team tab mount only — list churn is low-frequency and was the explicit reason `8c4f9a1` removed the manual refresh button. Implementation choice: either expose a `startAutoRefresh(name)`/`stopAutoRefresh()` pair on the store that `MissionPanel` calls in `useEffect`, or add the `setInterval` inside `MissionPanel` and call `loadMission` directly with a "silent" option added to the store action. Pick the second — keeps store API surface small and refresh policy in the view.
- Reason: User feedback "看板信息需要定时刷新" (Kanban info needs periodic refresh). Without auto-refresh, Hub users watching agents work must manually re-select the Mission to see updated task state, which contradicts Team tab's role as a passive observation surface. 15s is the cadence that matches typical agent task move-to-Done latency without spamming the file read on the server.
- Acceptance: With Team tab open and a Mission selected, modifying `kanban.md` on disk produces an updated Kanban view within ~15s without user interaction. Switching browser tab away pauses the refresh (verifiable via server logs showing no `/api/missions/:name` calls from this client while hidden), returning resumes it. The Mission selector dropdown does not flicker disabled every 15s. Switching the selected Mission cancels the previous interval. `MissionPanel` unmount clears the interval. Add a unit test in `missionStore.test.ts` that calls the silent-refresh path and asserts state updates without `isLoading` toggling, plus a `MissionPanel.test.ts` test that mounts the panel with a selected Mission and uses `vi.useFakeTimers()` to assert the loadMission action fires after 15s and stops after unmount. `pnpm -C packages/web exec vitest run src/components/missions src/stores` passes. `pnpm -C packages/web exec tsc --noEmit` passes.
- Result: Added 15s selected Mission detail auto-refresh while Team panel is mounted. `MissionPanel` starts a cleanup-safe interval for the selected Mission, skips hidden tabs and visible user-loading states, resumes on `visibilitychange`, and calls `loadMission(name, { silent: true })` so the selector does not flicker disabled. `missionStore.loadMission` now supports silent detail reloads without toggling `isLoading`.
- Files: `packages/web/src/stores/missionStore.ts`; `packages/web/src/stores/missionStore.test.ts`; `packages/web/src/components/missions/MissionPanel.tsx`; `packages/web/src/components/missions/MissionPanel.test.ts`
- Verification: `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts src/components/missions/MissionPanel.test.ts` failed before implementation on silent-loading/timer assertions, then passed, 13 tests. `pnpm -C packages/web exec vitest run src/components/missions src/stores` passed, 23 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/web build` passed with existing large-chunk warnings.
- Review: accepted by: Squad Lead, 2026-05-08 - `loadMission(name, { silent: true })` now bypasses the `isLoading` toggle, so the auto tick does not flicker the selector. `MissionPanel` `useEffect` starts a 15s `setInterval` keyed on `selectedMission.name`, skips ticks while `document.hidden`, skips while a user-initiated `isLoading` is in flight, and tears down on unmount or when the selected Mission changes; `visibilitychange` listener resumes on return. Mission *list* refresh remains mount-only, preserving `8c4f9a1`'s removal of the manual refresh affordance. Verified vitest store/panel suite (13/13), full `src/components/missions src/stores` suite (23/23), `tsc --noEmit` clean, web build clean.
- Updated: 2026-05-08, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/components/missions/MissionOverview.tsx`, `packages/web/src/components/missions/MissionPanel.tsx`, `packages/web/src/components/missions/MissionKanban.tsx`, `packages/web/src/styles/globals.css`
- Ref: f1a8e62
- Request: Three coordinated UI changes to the Mission observation surface:
  (1) **Slim Mission overview**: in `MissionOverview.tsx`, remove the four `Stat` count cards (`To Claim`, `In Progress`, `Done`, `Unreviewed`) and the multi-line `acceptance` summary block. Keep Mission name, lifecycle pill, date, and the single intent paragraph. Counts already appear in the Kanban column headers, so duplicating them in the overview is noise. The lifecycle/file-incomplete warning lines (`mission.incomplete`, `kanban.error`) stay — those are surface-critical, not stats.
  (2) **Vertical Mission Agents layout**: in `MissionPanel.tsx` `ObservationShell`, stop laying out `MissionKanban` and `MissionAgents` in a 2-column grid (`gridTemplateColumns: 'minmax(0, 1fr) 300px'`). Replace with a vertical flex stack: Kanban occupies its natural height as the primary block, then Mission Agents renders as its own full-width row below. The container should still scroll vertically when content overflows. Adjust `MissionAgents` styling so its agent list flows horizontally (`flex` with wrap) at full width instead of the current single-column 300px-wide layout.
  (3) **Kanban card display optimization**: in `MissionKanban.tsx` task cards, reduce visual noise. Concrete changes: collapse `To: X` and `From: Y` into a single compact meta line (e.g. `X ← Y` or `To X · From Y`); hide the `Acceptance` block from the card by default (it's verbose; keep it accessible via expand-on-click or move it into a tooltip on `request`); replace the trailing `Reviewed` / `Review pending` text label with a small colored badge (green = reviewed, amber = pending) aligned with `updated`; ensure the card top-row `ref` strong + open-source button stays. Card should remain readable at 280–360px column width.
  Use the existing parser data from `missionStore` — no parser/store changes for this task. Keep all behavior changes inside the three components plus targeted CSS additions in `globals.css` (and updates/removals for now-unused `mission-onboarding`-adjacent or stat-card classes only if they were exclusive to the removed UI).
- Reason: User feedback after Phase 3 dogfooding — "mission 描述保留大致的任务说明即可,数字卡片和文件介绍有些多余" (overview has redundant stat cards), "mission Agent 不要和 kanban 放在一行,纵向增加一个模块" (Agents column squeezes Kanban width and competes for attention), "看板卡片信息展示优化" (cards too dense). Goal is observation surface that reads at a glance: name + intent at top, Kanban as the largest block, Mission Agents underneath as a secondary row.
- Acceptance: `MissionOverview` no longer renders the four stat cards or the acceptance summary list — only name, lifecycle pill, date, and intent paragraph (plus the existing incomplete/parse warning lines). `ObservationShell` lays out Kanban above Mission Agents vertically; at typical Hub editor widths Kanban uses full editor width and Agents wraps below it. Kanban task cards show: ref + open-source on top row, a single combined To/From meta line, scope, request, footer with `updated` text and a colored review badge — no Acceptance block visible by default. Updated `MissionPanel.test.ts` (or new test file) asserts: (a) overview HTML does not contain "To Claim" / "In Progress" / "Done" stat labels in the overview region, (b) MissionPanel snapshot/structure places `MissionAgents` after `MissionKanban` in DOM order without a side-by-side grid, (c) a sample task card renders the combined meta line. `pnpm -C packages/web exec vitest run src/components/missions src/stores` passes. `pnpm -C packages/web exec tsc --noEmit` passes. `pnpm --filter @nexus/web build` passes. Smoke-test with `pnpm run dev:full` at a 1366×768 viewport: file tree column visible, no horizontal page scroll.
- Result: Slimmed `MissionOverview` to Mission name, lifecycle, date, intent, and existing warning lines by removing duplicated task stat cards and acceptance summary text. Changed `ObservationShell` from a Kanban/Agents side grid to a vertical flex stack so Kanban uses full width and Mission Agents renders below. Optimized Kanban task cards with a single combined To/From meta chip, no visible Acceptance block, and a compact green/amber review badge aligned with updated text. Updated Mission Agent list CSS to wrap horizontally at full width.
- Files: `packages/web/src/components/missions/MissionOverview.tsx`; `packages/web/src/components/missions/MissionOverview.test.ts`; `packages/web/src/components/missions/MissionPanel.tsx`; `packages/web/src/components/missions/MissionPanel.test.ts`; `packages/web/src/components/missions/MissionKanban.tsx`; `packages/web/src/components/missions/MissionKanban.test.ts`; `packages/web/src/styles/globals.css`
- Verification: `pnpm -C packages/web exec vitest run src/components/missions/MissionOverview.test.ts src/components/missions/MissionKanban.test.ts src/components/missions/MissionPanel.test.ts` failed before implementation on overview stat/acceptance duplication, Kanban card density, and side-grid layout assertions, then passed, 6 tests. `pnpm -C packages/web exec vitest run src/components/missions src/stores` passed, 21 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/web build` passed with existing large-chunk warnings. `NEXUS_PORT=7800 pnpm run dev:full` started web at `http://localhost:7701/` and server at `http://localhost:7800`; the process was then stopped with Ctrl-C.
- Review: accepted by: Squad Lead, 2026-05-08 - `MissionOverview` now renders only Mission name, lifecycle pill, date, and intent paragraph plus the existing incomplete/parse warning lines; the four `Stat` count cards and the acceptance summary block are gone (counts already live in Kanban column headers). `ObservationShell` (MissionPanel.tsx:118) is a vertical flex stack — `MissionKanban` first, `MissionAgents` second — replacing the prior `gridTemplateColumns: 'minmax(0, 1fr) 280px'` side layout, so Kanban uses full editor width and Agents flows below as its own row. Kanban task cards collapse `To`/`From` into a single meta line, drop the visible Acceptance block, and use a colored badge (green = reviewed, amber = pending) aligned with `updated`. Note: this supersedes `f2a91d6`'s "300px right-side column" framing — the two layout decisions are mutually exclusive and the vertical stack matches the dispatched intent. Verified vitest overview/kanban/panel (6/6), full `src/components/missions src/stores` suite (21/21), `tsc --noEmit` clean, web build clean.
- Updated: 2026-05-08, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/components/missions/MissionAgents.tsx`, `packages/web/src/components/missions/MissionPanel.tsx`, `packages/web/src/styles/globals.css`
- Ref: f2a91d6
- Request: Restyle the Mission Agents observation column so it reads like a team-roster intro instead of a debug dump. Three concrete changes: (1) Keep `MissionAgents` rendered as its own dedicated right-side column in `MissionPanel.ObservationShell` (it already is via `gridTemplateColumns: 'minmax(0, 1fr) 280px'`) — verify the column renders cleanly when the kanban scrolls and the agent list is long, and consider widening to `300px` if cards feel cramped after the redesign. (2) Remove the Activation and Initial prompt summary blocks from each agent card entirely — drop the two `<div className="mission-agent-prompt">…</div>` sections in `MissionAgents.tsx`. The card no longer needs to surface activation/initial prompts; users open `agents.md` for that. The store fields `activationPromptSummary` / `initialPromptSummary` can stay populated (don't change the parser) — just stop rendering them. (3) Restyle each card to feel like a team-member profile intro: an avatar-style monogram or `Bot` icon disc on the left, agent name as the primary heading, a short responsibility paragraph as the body, and a compact task-count footer (e.g. `2 to claim · 1 active · 4 done`). Use `var(--bg-secondary)` / `var(--border-subtle)` so the card visually matches the rest of the Mission panels but feels distinct from a kanban task card. Avatar can be a CSS-only colored circle with the first letter of `agent.name` (deterministic color from a small palette keyed by name hash, or a single accent color — pick the simpler option). Keep the existing parser-fallback path (`!agents.ok`) and the empty path (`agents == null`) untouched aside from any shared header tweaks.
- Reason: User dogfooding feedback ("mission Agents 单独一列展示, 另外不需要展示初始和恢复提示词, 卡片的样式做成类似团队员工介绍的感觉"). The current card stacks responsibility + counts + two prompt summary blocks, which makes the column feel like a debug panel rather than a roster. Roster-style cards reinforce that Team tab is an observation surface — who is on the squad, what they own — without exposing prompt text that already lives in `agents.md`.
- Acceptance: `MissionAgents` cards no longer render any activation or initial prompt summary text. Each card shows: avatar/monogram, agent name, responsibility paragraph, and an inline task-count footer. The Mission Agents column remains a dedicated right-side column in `ObservationShell` and does not collide with the kanban at typical Hub viewport widths (verify around 1366×768 and 1920×1080 with default panel widths). No Start Pane / Send Prompt / Claim controls are introduced. Parser fallback (`!agents.ok`) and empty state (`agents == null`) still render. Existing tests pass: `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts src/components/missions/MissionPanel.test.ts src/components/missions/MissionCreateDialog.test.ts src/stores/workspaceStore.test.ts`. `pnpm -C packages/web exec tsc --noEmit` passes. If a snapshot or DOM assertion in any existing test references the prompt summary blocks, update the test to match the new card structure.
- Result: Restyled `MissionAgents` as roster-style team cards with a monogram avatar, agent heading, responsibility body, and compact task-count footer. Removed Activation and Initial prompt summary rendering while leaving parser/store fields intact for other flows. Kept the parser fallback and empty states unchanged, and widened the dedicated Team observation side column to 300px so the roster cards have more breathing room beside the Kanban.
- Files: `packages/web/src/components/missions/MissionAgents.tsx`; `packages/web/src/components/missions/MissionAgents.test.ts`; `packages/web/src/components/missions/MissionPanel.tsx`; `packages/web/src/styles/globals.css`; `agent-team/missions/hub-agent-team-mission-mvp/kanban.md`
- Verification: `pnpm -C packages/web exec vitest run src/components/missions/MissionAgents.test.ts` failed before implementation because roster avatar markup was missing and prompt summaries rendered, then passed. `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts src/components/missions/MissionAgents.test.ts src/components/missions/MissionPanel.test.ts src/components/missions/MissionCreateDialog.test.ts src/stores/workspaceStore.test.ts` passed, 15 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/web build` passed with the existing large-chunk warning. `rg -n "Start Pane|Send Prompt|Claim|Complete|Review|pane\\.create|onCreate|launch|mission-agent-prompt|Activation|Initial" packages/web/src/components/missions/MissionAgents.tsx packages/web/src/components/missions/MissionPanel.tsx packages/web/src/components/missions/MissionKanban.tsx packages/web/src/styles/globals.css` found only read-only status/label text, not Team Agent launch controls or prompt summary rendering.
- Review: accepted by: Squad Lead, 2026-05-08 - `MissionAgents.tsx` no longer renders `mission-agent-prompt` blocks; each card now wraps a `mission-agent-avatar` monogram disc plus a `mission-agent-profile` body (name + total, responsibility paragraph, and `mission-agent-counts` chips for to claim / active / done). Parser fallback (`!agents.ok`) and empty (`agents == null`) paths are unchanged. `MissionPanel.ObservationShell` widened the dedicated right column to `minmax(0, 1fr) 300px`. CSS uses `var(--bg-secondary)` cards, `var(--border-subtle)` borders, and an accent-mixed avatar background so the roster reads as profile cards. Verified `pnpm -C packages/web exec vitest run src/components/missions/MissionAgents.test.ts` 1/1 — asserts avatar, monogram `V`, agent name, responsibility, count chips, and that activation/initial prompt summaries are not rendered.
- Updated: 2026-05-08, Squad Lead

To: Marbas | From: User | Scope: `packages/web/src/components/AddPaneDialog.tsx`, `packages/web/src/components/AgentPane.tsx`, Hub workspace pane list container, `packages/server/src/workspace/WorkspaceManager.ts`, pane title API or WebSocket handler
- Ref: 9d4b6c1
- Request: Add or verify Mission context in pane creation and pane observation without adding Team card launch controls.
- Reason: Mission-associated panes should be creatable from Add Pane, observable in pane cards, filterable in the Hub pane list, and renameable without mutating Mission metadata.
- Acceptance: Add Pane can select an Agent from the active Mission and fill pane name/task. Pane cards show mission name, role, and mission agent name. Pane list can filter by Mission and agent type. Pane title editing persists and does not change mission metadata. Team Agent cards do not gain launch controls.
- Result: Added focused regression coverage for Mission-aware Add Pane defaults, pane mission metadata construction, Hub pane Mission/agent filtering, and WorkspaceManager title persistence. Extracted pure helpers for Mission Agent pane defaults and Hub pane filters so those behaviors can be tested without loading the full terminal UI. Existing pane card badges and WebSocket rename flow remain intact; rename still updates only pane title while preserving mission metadata and pane config.
- Files: `packages/web/src/components/AddPaneDialog.tsx`; `packages/web/src/components/AddPaneDialog.test.ts`; `packages/web/src/components/Layout.tsx`; `packages/web/src/components/Layout.test.ts`; `packages/web/src/components/hubPaneFilters.ts`; `packages/server/src/workspace/WorkspaceManager.test.ts`; `agent-team/missions/hub-agent-team-mission-mvp/kanban.md`
- Verification: `pnpm -C packages/web exec vitest run src/components/AddPaneDialog.test.ts src/components/Layout.test.ts` failed first on missing Mission pane helper exports and missing pane filter helper, then passed with 4 tests. `pnpm -C packages/server test src/workspace/WorkspaceManager.test.ts` passed with 1 test. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/server build` passed. `pnpm --filter @nexus/web build` passed with the existing large-chunk warning. `rg -n "Start Pane|Send Prompt|Claim|Complete|Review|pane\\.create|onCreate|launch" packages/web/src/components/missions/MissionAgents.tsx packages/web/src/components/missions/MissionPanel.tsx packages/web/src/components/missions/MissionKanban.tsx` found only read-only labels/status text, not Team Agent launch controls.
- Review:
- Updated: 2026-05-08, Marbas

To: Samigina | From: Samigina | Scope: `packages/web/src/components/missions/MissionKanban.tsx`, `packages/web/src/components/missions/MissionAgents.tsx`, `packages/web/src/stores/missionStore.ts`
- Ref: 9a7d2e4
- Request: Verify and harden the read-only Mission observation components and parsers against the current acceptance criteria.
- Reason: The active prompt requested Samigina's Mission observation scope after the original tasks had already landed and been reviewed; this follow-up records the current run without reopening accepted task refs.
- Acceptance: Kanban parsing covers To Claim, In Progress, and Done. Team observation UI exposes no Claim, Complete, or Review mutation controls. Mission Agents parsing surfaces names, responsibilities, prompt summaries, and task counts. Parser failures preserve raw fallback content without writing Mission files.
- Result: Hardened Kanban source-line parsing so duplicate task blocks in different status sections keep independent line numbers for source opening. Existing read-only Kanban and Mission Agents observation behavior remains unchanged: the components expose filters, source links, raw parser fallbacks, summaries, and counts, but no Claim, Complete, Review, Start Pane, or Send Prompt operation controls.
- Files: `packages/web/src/stores/missionStore.ts`; `packages/web/src/stores/missionStore.test.ts`; `agent-team/missions/hub-agent-team-mission-mvp/kanban.md`
- Verification: `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts` failed first on duplicate task source-line regression, then passed with 7 tests. `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts src/components/missions/MissionPanel.test.ts src/components/missions/MissionCreateDialog.test.ts src/stores/workspaceStore.test.ts` passed, 14 tests. `rg -n "Claim|Complete|Review|Start Pane|Send Prompt|POST /api/missions|activateMission|createMission" packages/web/src/components/missions/MissionKanban.tsx packages/web/src/components/missions/MissionAgents.tsx packages/web/src/stores/missionStore.ts` found only read-only labels/status fields in Mission observation components; lifecycle create/activate actions remain in the store, not Kanban/Agent mutation controls. `pnpm -C packages/web exec tsc --noEmit` failed on unrelated current files: missing exports in `src/components/AddPaneDialog.test.ts`, missing `total` in that test fixture, and missing `./hubPaneFilters` plus implicit `any` in `src/components/Layout.test.ts`.
- Review: accepted by: Samigina, 2026-05-08 - Focused parser regression is covered by a red-green test, duplicate task source lines are now computed from match offsets instead of raw text lookup, mission observation tests pass, and the remaining type-check failures are in unrelated Marbas/Layout test files outside this scope.
- Updated: 2026-05-08, Samigina

To: Vassago | From: Squad Lead | Scope: `packages/web/src/styles/globals.css`, `packages/web/src/components/missions/MissionPanel.tsx`
- Ref: d8f3b95
- Request: Fix the onboarding placeholder Kanban overflow that pushes the right-side file tree off-screen on typical Hub viewports. Root cause: `.mission-onboarding-grid` uses `grid-template-columns: repeat(3, minmax(180px, 1fr))`, forcing a 540px+ minimum width inside an editor tab whose available width depends on the four-column Hub layout (sidebar + agent panes + editor + file tree). When the editor area is narrow, this grid overflows horizontally, the file tree column collapses or scrolls off-screen, and the placeholder is unreadable. Fix the grid to allow shrinking by switching to `minmax(0, 1fr)` and add an `overflow: hidden` boundary on the onboarding container so internal overflow stays inside the editor tab. Apply the same `min-width: 0` hygiene to `.mission-onboarding-column`, `.mission-onboarding-column-body`, `.mission-onboarding-overview`, `.mission-onboarding-overview-title`, `.mission-onboarding-stats`, `.mission-onboarding-card` content as needed so long card titles wrap instead of pushing parent width. Verify via `pnpm run dev:full`: at editor widths down to ~540px the file tree stays fully visible and the placeholder gracefully wraps cards into rows or scrolls vertically inside its container — never horizontally outside it.
- Reason: User feedback ("右侧文件树展示不出来了 ... 占位中的看板样式有溢出问题") — the onboarding placeholder caused a horizontal overflow regression that visually removed the file tree column. Empty state must not break the rest of the workspace layout.
- Acceptance: At a Hub viewport of 1366×768 with default panel widths, the file tree column is fully visible while the Team tab shows the empty-state onboarding. The 3-column Kanban placeholder either fits, or wraps gracefully (consider `repeat(auto-fit, minmax(180px, 1fr))`), or scrolls vertically — never horizontally past the editor tab boundary. No `overflow-x: scroll` or horizontal scrollbar appears at the page level. `pnpm -C packages/web exec vitest run src/components/missions/MissionPanel.test.ts src/components/missions/MissionCreateDialog.test.ts src/stores/missionStore.test.ts src/stores/workspaceStore.test.ts` passes. `pnpm -C packages/web exec tsc --noEmit` passes.
- Result: Fixed the onboarding preview overflow by bounding `.mission-onboarding` with hidden horizontal overflow plus vertical scrolling, changing the placeholder Kanban grid to `repeat(3, minmax(0, 1fr))`, and adding `min-width: 0` / overflow wrapping hygiene across the preview, overview, stats, columns, card bodies, and card text so long labels wrap inside the editor tab instead of expanding the workspace layout.
- Files: `packages/web/src/styles/globals.css`; `packages/web/src/components/missions/MissionPanel.test.ts`
- Verification: `pnpm -C packages/web exec vitest run src/components/missions/MissionPanel.test.ts` failed before implementation on the CSS overflow assertions, then passed, 3 tests. `pnpm -C packages/web exec vitest run src/components/missions/MissionPanel.test.ts src/components/missions/MissionCreateDialog.test.ts src/stores/missionStore.test.ts src/stores/workspaceStore.test.ts` passed, 13 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm run dev:full` failed under sandbox on `/tmp/tsx-0/42.pipe` EPERM; rerun outside sandbox started web at `http://localhost:7701/` but hit existing port `7700`; rerun as `NEXUS_PORT=7800 pnpm run dev:full` started web at `http://localhost:7701/` and server at `http://localhost:7800`, then was stopped with Ctrl-C.
- Review: accepted by: Squad Lead, 2026-05-07 - `.mission-onboarding` now bounds horizontal overflow with `min-width: 0` + `overflow: hidden` + `overflow-y: auto`, `.mission-onboarding-grid` switched to `repeat(3, minmax(0, 1fr))`, and `min-width: 0` / `overflow-wrap: anywhere` were applied across preview/overview/stats/columns/card body/card text. The placeholder no longer pushes the right-side file tree off-screen at narrow editor widths. Verified vitest `MissionPanel.test.ts` 3/3 (CSS assertions for `.mission-onboarding` overflow, `.mission-onboarding-grid` columns, `.mission-onboarding-card` min-width, and card text wrapping all pass), full mission suite 13/13, and `tsc --noEmit` clean.
- Updated: 2026-05-07, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/components/missions/MissionPanel.tsx`, `packages/web/src/components/missions/MissionSelector.tsx`, `packages/web/src/styles/globals.css`
- Ref: c4a1f72
- Request: Move the onboarding "Create Mission" CTA out of the `MissionOnboarding` placeholder body and into the `MissionSelector` slot so the create entry point sits next to the Mission dropdown in every empty/non-empty state. Concretely: in `MissionPanel`, stop passing `hideCreateButton={true}` when missions are empty so `MissionSelector` keeps showing its "New Mission" button. Remove the standalone CTA button from `MissionOnboarding` (drop the `onCreate` prop and the `<button className="apd-btn apd-btn-primary mission-onboarding-cta">` block). The `MissionCreateDialog` instance owned by `MissionSelector` becomes the only entry point. The intro section in `MissionOnboarding` should still keep its kicker/title/paragraph but no longer needs the right-side CTA, so revisit `mission-onboarding-intro` and `mission-onboarding-cta` styles to keep the intro readable without the button (left-aligned block, no `justify-content: space-between`).
- Reason: User feedback ("mission占位创建按钮位置不对同一挪到和mission选择器位置") — the empty-state CTA inside the placeholder is visually separated from the Mission dropdown, which is where users expect the "create" affordance. Co-locating both controls reduces UI surfaces and matches the non-empty Team tab where create lives next to the selector.
- Acceptance: With zero Missions in the workspace, `MissionSelector` renders the dropdown ("No Missions") plus the "New Mission" button; the placeholder body shows intro + faded preview only, with no CTA button inside it. Clicking the selector's New Mission button opens `MissionCreateDialog`. With at least one Mission present, the same selector continues to show "New Mission" as before (no regression). `MissionPanel.test.ts` placeholder data assertions still pass; update or extend a test if the onboarding component signature changes.
- Result: Moved Mission creation back to the selector as the only empty-state entry point. `MissionPanel` no longer hides the selector button, `MissionOnboarding` no longer owns a CTA or dialog state, and the onboarding intro now renders as a left-aligned text block without right-side button spacing.
- Files: `packages/web/src/components/missions/MissionPanel.tsx`; `packages/web/src/components/missions/MissionSelector.tsx`; `packages/web/src/components/missions/MissionPanel.test.ts`; `packages/web/src/styles/globals.css`
- Verification: `pnpm -C packages/web exec vitest run src/components/missions/MissionPanel.test.ts` failed before implementation because the empty state lacked `New Mission`, then passed, 2 tests. `rg -n "hideCreateButton|mission-onboarding-cta|Create Mission" packages/web/src/components/missions/MissionPanel.tsx packages/web/src/components/missions/MissionSelector.tsx packages/web/src/styles/globals.css` found no matches. `pnpm -C packages/web exec tsc --noEmit` passed.
- Review: accepted by: Squad Lead, 2026-05-07 - `MissionPanel` no longer passes `hideCreateButton`, `MissionSelector` always renders the New Mission button as the sole entry point and owns the only `MissionCreateDialog`. `MissionOnboarding` no longer takes `onCreate` and the `mission-onboarding-cta` class/button are gone. Intro now uses `display: block` / `max-width: 780px` so it stays readable without the prior right-aligned slot. Verified `pnpm -C packages/web exec vitest run src/components/missions/{MissionPanel,MissionCreateDialog}.test.ts src/stores/{missionStore,workspaceStore}.test.ts` 13/13, `tsc --noEmit` clean, and `rg "hideCreateButton|mission-onboarding-cta"` returns no matches.
- Updated: 2026-05-07, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/components/missions/MissionPanel.tsx`, `packages/web/src/components/missions/`
- Ref: b91c3df
- Request: Replace the current bare `CenterState` "No Missions" empty state in `MissionPanel` with a richer onboarding view that combines (a) a short feature introduction explaining what the Team tab is for (Markdown-backed Mission workspace, Squad Lead pane creation, Kanban observation, Mission Agents) and (b) a non-functional placeholder showcase that previews the layout users will see once they create a Mission — a faded/illustrative MissionOverview row, a 3-column read-only Kanban skeleton (To Claim / In Progress / Done) with a few example task cards, and a Mission Agents row with example agent cards. The placeholder must be visually distinct from the real panels (reduced opacity or watermark) so users do not mistake it for real data, and must contain a primary "Create Mission" CTA that opens the new creation dialog from `7e5b2c4`. No interactivity beyond the CTA.
- Reason: Empty state currently gives users nothing to anchor on. A feature intro + visual preview communicates Team tab value before the user has any Mission, and reduces the cold-start drop-off where users open Team tab, see "No Missions", and leave without understanding what the surface does.
- Acceptance: When `missions.length === 0` and `!isLoading`, MissionPanel renders the new onboarding layout with: a short intro paragraph, a placeholder MissionOverview, a placeholder Kanban (3 columns × ≥2 example cards each), a placeholder Mission Agents row (≥2 example cards), and one Create Mission CTA. Placeholder visuals are clearly non-real (opacity, dashed borders, or watermark). Clicking Create Mission opens the same creation dialog used by `MissionSelector`. The empty state never accidentally calls real Mission APIs or store actions. `pnpm -C packages/web exec tsc --noEmit` and `pnpm --filter @nexus/web build` pass.
- Result: Replaced the bare no-Missions center state with a Team onboarding shell. The empty state now explains the Markdown-backed Mission workspace, Squad Lead pane creation, Kanban observation, and Mission Agents, shows a clearly marked faded preview with Mission overview stats, three Kanban columns with example cards, and two Mission Agent cards, hides the selector create button while empty, and exposes a single onboarding Create Mission CTA that opens the shared `MissionCreateDialog`.
- Files: `packages/web/src/components/missions/MissionPanel.tsx`; `packages/web/src/components/missions/MissionSelector.tsx`; `packages/web/src/components/missions/MissionPanel.test.ts`; `packages/web/src/styles/globals.css`
- Verification: `pnpm -C packages/web exec vitest run src/components/missions/MissionPanel.test.ts` first failed before implementation, then passed. `pnpm -C packages/web exec vitest run src/components/missions/MissionCreateDialog.test.ts src/components/missions/MissionPanel.test.ts src/stores/missionStore.test.ts src/stores/workspaceStore.test.ts` passed, 11 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/web build` passed with existing large-chunk warnings.
- Review: accepted by: Squad Lead, 2026-05-07 - `MissionPanel.MissionOnboarding` renders the intro paragraph, three Kanban columns with two example cards each, two Mission Agent cards, and the watermark/dashed preview styling. CTA opens the shared `MissionCreateDialog`; the empty path does not call store mutation actions. `MissionSelector.hideCreateButton` is wired through `MissionPanel` so onboarding owns the only Create CTA when no Missions exist. Verified vitest run (11/11) and `tsc --noEmit` pass.
- Updated: 2026-05-07, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/components/missions/MissionSelector.tsx`
- Ref: 8c4f9a1
- Request: Remove the manual refresh button from `MissionSelector` (the `RefreshCw` icon button next to Mission selection). Mission list refresh should happen implicitly on Team tab mount, after `createMission` resolves, and after `activateMission` resolves — all of which already trigger refresh in the store. Do not introduce alternative manual refresh affordances elsewhere in the Mission UI.
- Reason: Mission list state changes are low-frequency (creation, activation). A persistent manual refresh button adds visual noise and implies the list is unreliable. Implicit refresh on the existing lifecycle actions covers all real cases.
- Acceptance: The refresh icon button is removed from `MissionSelector`. Creating or activating a Mission still refreshes the list. The Team tab still loads Mission data on mount. No console errors. `pnpm -C packages/web exec tsc --noEmit` passes. The unused `RefreshCw` import (if any) is also removed.
- Result: Removed the manual refresh icon button from `MissionSelector` and dropped the unused `RefreshCw` import/store selector. Mission data still refreshes through Team tab mount and the existing create/activate store flows.
- Files: `packages/web/src/components/missions/MissionSelector.tsx`
- Verification: `rg -n "RefreshCw|Refresh Missions|refresh\\(\\)" packages/web/src/components/missions/MissionSelector.tsx packages/web/src/components/missions packages/web/src/stores/missionStore.ts` found no manual selector refresh button and only the Team tab mount refresh. `pnpm -C packages/web exec tsc --noEmit` passed.
- Review: accepted by: Squad Lead, 2026-05-07 - `MissionSelector` no longer imports `RefreshCw` and exposes only the activate / new-Mission affordances. Mission list refresh still flows through `MissionPanel` mount + store `createMission`/`activateMission`. Verified `rg "RefreshCw" packages/web/src/components/missions` returns no matches and `tsc --noEmit` passes.
- Updated: 2026-05-07, Squad Lead

To: Bael | From: Squad Lead | Scope: `packages/server/src/hub/index.ts`, `packages/server/src/mission/MissionService.ts`, `packages/server/src/mission/routes.ts`
- Ref: e7b2c84
- Request: Fix Mission discovery being unreachable from the Hub UI. Root cause: the per-workspace server (`packages/server/src/index.ts`) calls `registerMissionRoutes(fastify, missionService)` and the user's running workspace process predates that wiring (PID started before 2026-05-06), so `/api/missions` falls through to the SPA `setNotFoundHandler` and returns `index.html`. The discovery code added in `a2d6e83` is correct but never runs. There are two parts: (1) Document that the running per-workspace server must be restarted for `a2d6e83` and `d3f8a57` to take effect — add a brief operator note in the Mission feature doc or `mission-workflow.md` if appropriate; (2) Also wire mission routes into the Hub server (`buildHubServer` in `packages/server/src/hub/index.ts`) using a `MissionService` constructed against the Hub's project working directory (i.e. the directory the Hub itself was started in, equivalent to `process.cwd()` or the resolved `cliEntry` workdir), so that a Hub user who has not yet connected to a workspace instance can still see Missions in the workspace where they ran `mexus hub`. The Hub-mounted `MissionService` should not have a `paneCreator` — pane creation is per-workspace, not Hub-level — so creation from the bare Hub path either is rejected with a clear 400 ("Connect to a workspace instance to create Missions") or only returns Mission files without spawning a Squad Lead pane. Pick rejection for simplicity and document the chosen behavior. Add a Hub-routes test that asserts `/api/missions` returns the on-disk Missions when called against the Hub server.
- Reason: User reported "已有mission 识别不到的问题没解决" — even after `a2d6e83`, the existing `hub-agent-team-mission-mvp` Mission still does not surface in the UI. Investigation shows the running per-workspace process is stale (started Apr26, before mission routes existed) so discovery never executes. Restarting fixes the connected case; wiring Hub-level routes also covers the disconnected/Hub-only case where a user opens Team tab from the Hub root before connecting to an instance.
- Acceptance: After restarting the per-workspace server, `curl http://localhost:<workspace-port>/api/missions` returns at least the `hub-agent-team-mission-mvp` Mission. After restarting the Hub server, `curl http://localhost:<hub-port>/api/missions` (the bare Hub path, not the proxy) also returns Missions discovered in the Hub's working directory. `POST /api/missions` against the Hub returns a 400 with a "Connect to a workspace instance to create Missions" message (or equivalent rejection reason). Existing Mission tests pass. Add a Hub-routes integration test. Operator note added to the appropriate Mission doc.
- Result: Wired Mission routes into `buildHubServer` with a Hub-local `MissionService` using the Hub process working directory. Bare Hub `GET /api/missions` now returns on-disk Missions from the directory where `mexus hub` was started. Bare Hub `POST /api/missions` is explicitly rejected with `Connect to a workspace instance to create Missions`, preserving pane creation as a per-workspace behavior. Added Hub route tests for both read-only discovery and create rejection, and documented the stale workspace process restart requirement plus Hub read-only behavior in the Mission integration design.
- Files: `packages/server/src/hub/index.ts`; `packages/server/src/hub/hubMissionRoutes.test.ts`; `packages/server/src/mission/routes.ts`; `design/agent-team/mexus-agent-team-mission-integration.md`
- Verification: `pnpm -C packages/server test src/hub/hubMissionRoutes.test.ts src/mission/MissionService.test.ts src/mission/routes.test.ts` passed, 14 tests. `pnpm -C packages/server test` passed, 107 tests. `pnpm --filter @nexus/server build` passed. `pnpm -C packages/server exec tsc --noEmit` was attempted but remains blocked by existing unrelated type errors in `src/hub/index.ts`, `src/index.ts`, script declaration tests, and `src/workspace/ConfigManager.ts`.
- Review: accepted by: Squad Lead, 2026-05-07 - `buildHubServer` now constructs a Hub-local `MissionService` against `process.cwd()` and registers the same Mission routes used by per-workspace, so `GET /api/missions` against the bare Hub returns on-disk Missions discovered in the directory where `mexus hub` was started. `POST /api/missions` is rejected at the Hub with a clear "Connect to a workspace instance to create Missions" message, preserving Squad Lead pane creation as a per-workspace concern. Hub route test `hubMissionRoutes.test.ts` covers both read-only discovery and create rejection (14/14 server tests), full server suite 107/107, server build passes. Pre-existing unrelated tsc errors are out of scope. Operator restart guidance for stale per-workspace processes added to `design/agent-team/mexus-agent-team-mission-integration.md`.
- Updated: 2026-05-07, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/components/missions/MissionSelector.tsx`, `packages/web/src/components/missions/`
- Ref: 7e5b2c4
- Request: Replace the current `window.prompt`-based Mission creation flow in `MissionSelector.handleCreate` with a proper modal dialog (e.g. `MissionCreateDialog.tsx`) that collects all Mission inputs in one form. The dialog must expose every field already supported by `missionStore.createMission` and the `POST /api/missions` payload: `name` (required, validated against the same name rules used server-side — non-empty, no path separators), `goal` (multi-line textarea), `constraints` (multi-line textarea), and `acceptance` (multi-line textarea). Show inline validation for empty/invalid name and disable the Create button while a creation is in flight. Submission should call `createMission` once with the full payload, close on success, and surface server errors inline (do not blank the dialog on failure). Keep the entry point a button on `MissionSelector`.
- Reason: Mission intent quality is set at creation time. The current `window.prompt` chain only captures name + goal, which forces users to repair Missions through file edits to add constraints/acceptance, and loses focus context between prompts. A single dialog reduces friction and makes Mission creation align with Mission template expectations.
- Acceptance: From the Team tab, the user can open a single dialog that collects name + goal + constraints + acceptance, submit once, and see the new Mission in the list with all four fields populated in `mission.md`. Empty/invalid name is rejected client-side before hitting the API. Dialog disables submit during the in-flight request and surfaces server errors inline. No new `window.prompt` calls remain in the Mission UI surface. No backend changes required (the API and store already accept all four fields).
- Result: Replaced the prompt chain with `MissionCreateDialog`, a modal form that collects name, goal, constraints, and acceptance in one submit. Client-side validation now rejects empty/invalid names before API calls, submit is disabled while invalid or creating, server errors remain inline without clearing the form, and successful creation closes the dialog. `missionStore.createMission` now returns success/failure so the dialog can make that decision cleanly.
- Files: `packages/web/src/components/missions/MissionCreateDialog.tsx`; `packages/web/src/components/missions/MissionCreateDialog.test.ts`; `packages/web/src/components/missions/MissionSelector.tsx`; `packages/web/src/stores/missionStore.ts`; `packages/web/src/styles/globals.css`
- Verification: `pnpm -C packages/web exec vitest run src/components/missions/MissionCreateDialog.test.ts` passed, 2 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `rg -n "window\\.prompt|prompt\\(" packages/web/src/components/missions packages/web/src/stores/missionStore.ts` found no matches.
- Review: accepted by: Squad Lead, 2026-05-07 - Dialog collects name/goal/constraints/acceptance in one submit, applies the same `^[A-Za-z0-9][A-Za-z0-9._-]*$` rule used by `MissionService.assertMissionName` for client-side validation, disables submit while invalid or in-flight, surfaces server errors inline without clearing the form, and `createMission` returns `boolean` so the dialog only closes on success. No `window.prompt` calls remain on the Mission UI surface. Verified vitest (11/11) and `tsc --noEmit`.
- Updated: 2026-05-07, Squad Lead

To: Bael | From: Squad Lead | Scope: `packages/server/src/mission/MissionService.ts`, `packages/server/src/mission/MissionService.test.ts`
- Ref: a2d6e83
- Request: Fix Mission discovery in `MissionService` so that Missions already present on disk under `agent-team/missions/<name>/` are returned by `GET /api/missions` even when they were not created through `MissionService.createMission`. Concretely: the project's existing Mission directory `agent-team/missions/hub-agent-team-mission-mvp/` (and any other already-on-disk mission folders such as `design/agent-team/example/missions/*`-style fixtures intended to be discoverable) is currently not surfacing in the Hub Team tab Mission list. Audit `discoverMissions` (or equivalent), confirm whether it (a) only scans a single root, (b) requires a marker that legacy missions lack, or (c) silently drops missions whose `summary` parse is partial. Pick the minimum-risk fix: Missions with at least `mission.md` present should appear in the list, with `summary.complete=false` and `missingFiles` populated when other required files are absent — they must not be hidden. Add unit tests that seed an `agent-team/missions/<name>/` directory containing only `mission.md` and assert the Mission appears in the discovery result with `complete: false` and the appropriate `missingFiles`. Do not change the create or activate paths.
- Reason: User reported "项目中已有的mission没识别出来" — the Phase 1 Mission `hub-agent-team-mission-mvp` itself does not appear in the Hub Team Mission list, so users cannot observe the Mission they're operating on. Discovery currently looks too strict; observation surface must tolerate partial/legacy Mission folders so they show up as incomplete rather than disappear.
- Acceptance: After running the server in this repository, `GET /api/missions` returns at least the `hub-agent-team-mission-mvp` Mission. Hub Team tab shows it in the Mission selector. A Mission folder containing only `mission.md` appears in the list with `complete: false` and accurate `missingFiles`. Existing 9af2e15 store contract is preserved (no shape change to `MissionSummary`). All existing `MissionService` tests still pass. `pnpm -C packages/server test src/mission/MissionService.test.ts src/mission/routes.test.ts` and `pnpm -C packages/server test` pass.
- Result: Audited `MissionService.listMissions` and found it scanned only `agent-team/missions/*` directories and treated any directory as a Mission, even without `mission.md`. Updated discovery to match the Mission file model: only valid Mission names with `agent-team/missions/<name>/mission.md` are listed, while Missions missing the other required files still surface as `complete: false` with accurate `missingFiles`. Added service and REST tests for on-disk Missions not created through `MissionService.createMission`, including the existing `hub-agent-team-mission-mvp` Mission name.
- Files: `packages/server/src/mission/MissionService.ts`; `packages/server/src/mission/MissionService.test.ts`; `packages/server/src/mission/routes.test.ts`
- Verification: `pnpm -C packages/server test src/mission/MissionService.test.ts src/mission/routes.test.ts` passed, 12 tests. `pnpm -C packages/server test` passed, 105 tests. `pnpm --filter @nexus/server build` passed. `pnpm -C packages/server exec tsc --noEmit` was attempted but remains blocked by existing unrelated type errors in `src/hub/index.ts`, script declaration tests, and `src/workspace/ConfigManager.ts`.
- Review: accepted by: Squad Lead, 2026-05-07 - `MissionService.listMissions` (MissionService.ts:226) now treats `agent-team/missions/<name>/mission.md` as the discovery marker, accepts on-disk Missions not created via `createMission`, and `readSummary` populates `missingFiles` so partial Missions surface as `complete: false` rather than disappearing. The existing `hub-agent-team-mission-mvp` Mission will appear in the Hub Team tab list. No changes to create/activate paths. Pre-existing unrelated server tsc errors remain out of scope. Verified `pnpm -C packages/server test src/mission/MissionService.test.ts src/mission/routes.test.ts` (12/12).
- Updated: 2026-05-07, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/stores/missionStore.ts`
- Ref: 9af2e15
- Request: Reconcile `missionStore` with the live `MissionService` API contract. The server returns `MissionDetail = { summary, files: Record<MissionFileKey, { path, exists, raw, parseError? }>, kanban }` where file keys are `mission|agents|kanban|roundtable|squadLead` (not `mission.md|agents.md|kanban.md`) and uses `summary.complete: boolean` (not `incomplete`). Update `normalizeMissionDetails` and `readMissionFile` so observation panels actually render against `/api/missions/:name` and `/api/missions/active` responses. Map `summary.complete=false` to a UI-friendly `incomplete=true` flag (or change consumers) so `MissionPanel`, `MissionSelector`, and `MissionOverview` react to incomplete Missions correctly. Keep raw fallback behavior — if `files.kanban.parseError` is present, surface it.
- Reason: Phase 1 acceptance requires Hub Team tab to display Mission overview, Kanban, and Mission Agents from the live API. Right now `kanban`, `agents`, and `overview` derive from `details.files['mission.md']`/`['kanban.md']`/`['agents.md']` which are always undefined under the actual server response — so those panels stay empty in the browser even though the parsers and components are correct in isolation.
- Acceptance: After a Mission is created or selected, `MissionOverview`, `MissionKanban`, and `MissionAgents` render real data from the server; toggling between Missions updates them; `selectedMission?.incomplete` correctly drives the warning panel for Missions with missing files; existing parser unit tests still pass; add a small store-level test that feeds a `MissionDetail` server-shape payload and asserts `kanban.ok`, `agents.ok`, and overview fields are populated.
- Result: Reconciled `missionStore` with the live `MissionService` detail shape. Store normalization now maps `summary.complete` to UI `incomplete`, preserves `missingFiles`, reads raw Markdown from extensionless server file keys (`mission`, `agents`, `kanban`, `roundtable`, `squadLead`), still supports the previous string-map shape, and surfaces `files.kanban.parseError` as the Kanban raw fallback error.
- Files: `packages/web/src/stores/missionStore.ts`; `packages/web/src/stores/missionStore.test.ts`
- Verification: `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts` passed, 6 tests. `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts src/stores/workspaceStore.test.ts` passed, 8 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/web build` passed with existing large-chunk warnings.
- Review: accepted by: Squad Lead, 2026-05-06 - `normalizeMissionDetails` maps `summary.complete` to `incomplete`, `readMissionFile` resolves both extensionless server keys and legacy `*.md` keys, kanban `parseError` is forwarded to the raw fallback, and the new live-payload tests cover both healthy and incomplete-mission paths. Resolves contract drift identified in audit.
- Updated: 2026-05-06, Squad Lead

To: Bael | From: Squad Lead | Scope: Mission creation flow, `packages/server/src/workspace/WorkspaceManager.ts`
- Ref: f0b6e39
- Request: Create a Squad Lead pane during Mission creation using the default CLI agent type. Fill pane task with the Skill activation prompt and Mission required reading, and attach pane mission metadata.
- Reason: A newly created Mission needs an immediate Squad Lead pane to finish decomposition and operate the Mission files.
- Acceptance: Created pane name uses `Squad Lead - hub-agent-team-mission-mvp` or the created Mission name. Pane task includes required Mission files. Pane card shows mission badge after creation.
- Blocked by: `b7d2a11` (Agares — pane mission metadata types must land first) and `e5a12cd` (Agares — default Mission CLI agent setting must exist before pane spawn). Do not claim until both refs are Done.
- Result: Mission creation now creates a Squad Lead pane when `MissionService` is wired with `WorkspaceManager`. The pane uses the configured `mission_defaults.agent_type` with `claudecode` as the configured fallback, is named `Squad Lead - <mission-name>`, uses `manual` restore in the shared workspace, receives the Skill activation prompt plus all required Mission files, and persists squad-lead Mission metadata for pane badges. `POST /api/missions` now awaits Mission creation so pane creation completes before the API returns.
- Files: `packages/server/src/mission/MissionService.ts`; `packages/server/src/mission/MissionService.test.ts`; `packages/server/src/mission/routes.ts`; `packages/server/src/index.ts`
- Verification: `pnpm -C packages/server test src/mission/MissionService.test.ts src/mission/routes.test.ts` passed, 10 tests. `pnpm -C packages/server test` passed, 103 tests. `pnpm --filter @nexus/server build` passed. `pnpm -C packages/server exec tsc --noEmit` was attempted but remains blocked by existing unrelated type errors in `src/hub/index.ts`, script declaration tests, and `src/workspace/ConfigManager.ts`.
- Review: accepted by: Squad Lead, 2026-05-06 - `MissionService.createSquadLeadPane` (MissionService.ts:406) names the pane `Squad Lead - <name>`, attaches `mission` metadata with role `squad-lead`, uses `restore: 'manual'` and `isolation: 'shared'`, resolves the agent from `mission_defaults.agent_type` with `claudecode` fallback and rejects `__shell__`/unconfigured agents. `buildSquadLeadTask` includes all seven required reading paths. `POST /api/missions` awaits createMission so the pane creation completes before the API responds. Pre-existing unrelated tsc errors are out of scope.
- Updated: 2026-05-06, Squad Lead

To: Agares | From: Squad Lead | Scope: `packages/server/src/types.ts`, `packages/web/src/types.ts`, `.nexus/config.yaml` serialization
- Ref: b7d2a11
- Request: Add Mission lifecycle and config shape. Workspace config should support `active_mission?: string` and pane mission metadata. Global or config defaults should support `mission_defaults?: { agent_type?: AgentType }`.
- Reason: Mission lifecycle, default CLI selection, and pane badges need typed state that survives restart without breaking old config files.
- Acceptance: Existing workspace configs still load. New fields persist without dropping existing pane fields. Pane mission metadata survives Mexus restart.
- Result: Added typed `active_mission?: string` support to workspace config, normalized legacy workspace configs without Mission fields, preserved pane Mission metadata through workspace config save/load, and normalized `mission_defaults` in global config.
- Files: `packages/server/src/types.ts`; `packages/web/src/types.ts`; `packages/server/src/workspace/ConfigManager.ts`; `packages/server/src/workspace/ConfigManager.test.ts`; `packages/server/src/mission/MissionService.ts`
- Verification: `pnpm -C packages/server test src/workspace/ConfigManager.test.ts src/mission/MissionService.test.ts` passed, 14 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/server build` passed. `pnpm --filter @nexus/web build` passed with existing large-chunk warnings.
- Review: accepted by: Squad Lead, 2026-05-06 - `PaneMission`, `active_mission`, and `mission_defaults` typed in both server and web `types.ts`. `ConfigManager` defaults `mission_defaults: {}`, normalizes legacy workspace configs without `active_mission`, and preserves `mission` on pane configs through save/load. Migration path covered by tests. Compatible with Marbas' earlier `PaneMission` shape — no conflict.
- Updated: 2026-05-06, Squad Lead

To: Agares | From: Squad Lead | Scope: `packages/server/src/workspace/ConfigManager.ts`, `packages/web/src/components/SettingsDialog.tsx`
- Ref: e5a12cd
- Request: Add Settings UI and config handling for Mission default CLI agent type. Reuse existing configured agent definitions as options and validate availability before creating a Squad Lead pane.
- Reason: Mission creation should not ask for an agent type every time; it should use a system default selected in Settings.
- Acceptance: User can pick the default Mission CLI agent in Settings. The setting persists in global config. Mission creation warns if the configured CLI is unavailable.
- Result: Added Mission default agent selection to Settings using configured non-shell agents, persisted it through `mission_defaults.agent_type`, displayed unavailable-agent install guidance from agent availability data, and sanitized invalid or shell Mission defaults in `ConfigManager`.
- Files: `packages/server/src/workspace/ConfigManager.ts`; `packages/server/src/workspace/ConfigManager.test.ts`; `packages/web/src/components/SettingsDialog.tsx`; `packages/server/src/types.ts`; `packages/web/src/types.ts`
- Verification: `pnpm -C packages/server test src/workspace/ConfigManager.test.ts src/mission/MissionService.test.ts` passed, 14 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/server build` passed. `pnpm --filter @nexus/web build` passed with existing large-chunk warnings.
- Review: accepted by: Squad Lead, 2026-05-06 - SettingsDialog reads/writes `mission_defaults.agent_type` from configured agents (no `__shell__`), `ConfigManager` sanitizes invalid/shell defaults at load, persists in global config, and shows install guidance from availability data. Mission creation will warn through the availability path before pane spawn.
- Updated: 2026-05-06, Squad Lead

To: Marbas | From: Squad Lead | Scope: `packages/web/src/components/AddPaneDialog.tsx`
- Ref: 5d8a2ef
- Request: Add Mission Agent choice to Add Pane. If an active Mission exists, show a Mission Agent selector. Selecting a Mission Agent fills pane name and task, keeps agent type defaulted from Mission default CLI setting unless changed, and attaches pane mission metadata on create.
- Reason: Creating a Mission-associated pane should happen in the pane creation flow, not from Team Agent cards.
- Acceptance: Add Pane can create a pane associated with active Mission Agent. Users can still create normal panes with no Mission. Team Agent cards are not used as launch buttons.
- Result: Add Pane now loads the active Mission, shows a Mission Agent selector when parsed Mission agents are available, fills pane name and task from the selected Mission Agent, honors the configured Mission default CLI agent until the user changes agent type, and sends pane mission metadata only for Mission-associated panes.
- Files: `packages/web/src/components/AddPaneDialog.tsx`; `packages/web/src/types.ts`; `packages/server/src/types.ts`
- Verification: `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/server build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - Selector only renders when an active Mission with parsed agents exists, fill action populates name and task, agent type defaults from Mission default CLI but remains user-overridable, and pane mission metadata is attached only on Mission-associated creation. Normal pane creation path is unchanged.
- Updated: 2026-05-06, Squad Lead

To: Marbas | From: Squad Lead | Scope: `packages/web/src/components/AgentPane.tsx`, pane type definitions
- Ref: 6ee91b4
- Request: Show Mission badges on pane cards. Display mission name, mission role, and mission agent name compactly.
- Reason: Hub users need to observe pane ownership and Mission association at a glance.
- Acceptance: Mission panes are visually distinguishable. Non-Mission panes are unchanged.
- Result: Pane cards now display a compact Mission badge with mission name, role, and Mission Agent name when pane mission metadata exists. Non-Mission panes render without the badge.
- Files: `packages/web/src/components/AgentPane.tsx`; `packages/web/src/styles/globals.css`; `packages/web/src/types.ts`; `packages/server/src/types.ts`; `packages/server/src/workspace/WorkspaceManager.ts`
- Verification: `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/server build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - `PaneMission` (name/path/role/agentName) typed in both server and web `types.ts`, badge renders only when metadata is present, non-Mission panes are visually unchanged, and the badge is read-only with no Mission state mutation surface.
- Updated: 2026-05-06, Squad Lead

To: Marbas | From: Squad Lead | Scope: Hub workspace pane list container, workspace store or local UI state
- Ref: 71a6d0c
- Request: Add pane filters for Mission and agent type. Support all / no mission / mission name and all / configured agent type.
- Reason: Hub Mission work creates multiple panes; users need low-friction observation filters without altering pane lifecycle.
- Acceptance: Filtering hides and shows panes without closing or restarting them. Filters do not modify workspace config.
- Result: Hub pane list now has local Mission and agent type filters. Mission filtering supports all, no mission, and discovered mission names from pane metadata. Agent filtering supports all and currently visible configured pane agent types. Filtering only affects rendering.
- Files: `packages/web/src/components/Layout.tsx`; `packages/web/src/styles/globals.css`
- Verification: `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/server build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - Filters are local UI state in `Layout.tsx`, support all / no mission / discovered Mission names and all / configured agent types, and only narrow rendering. Workspace config and pane lifecycle are not mutated.
- Updated: 2026-05-06, Squad Lead

To: Marbas | From: Squad Lead | Scope: `packages/web/src/components/AgentPane.tsx`, `packages/server/src/workspace/WorkspaceManager.ts`, API or WebSocket handler
- Ref: 8c3f45d
- Request: Support pane title editing. Add inline pane title edit, persist title to workspace config, and add a server handler such as `PATCH /api/panes/:paneId/title` or equivalent WebSocket event.
- Reason: Mission panes need user-adjustable titles while preserving mission metadata and pane configuration.
- Acceptance: User can rename a pane. Rename survives Mexus restart. Rename does not alter mission metadata, agent type, workdir, restore mode, or task.
- Result: Added inline pane title editing in pane cards and a `pane.rename` / `pane.renamed` WebSocket flow. `WorkspaceManager.renamePane` updates only `PaneState.name` and persisted `PaneConfig.name`, preserving mission metadata and the rest of the pane configuration.
- Files: `packages/web/src/components/AgentPane.tsx`; `packages/web/src/components/WorkspaceApp.tsx`; `packages/web/src/stores/workspaceStore.ts`; `packages/web/src/styles/globals.css`; `packages/web/src/types.ts`; `packages/server/src/types.ts`; `packages/server/src/workspace/WorkspaceManager.ts`; `packages/server/src/ws/handlers.ts`
- Verification: `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/server build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - `WorkspaceManager.renamePane` (line 217) mutates only `pane.name` and persists via `updatePaneConfigName`, leaving `agent`, `workdir`, `task`, `restore`, and `mission` untouched; rename persists through workspace config so it survives restart. `pane.rename` WS event wired in `handlers.ts:144` with `pane.renamed` echo.
- Updated: 2026-05-06, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/stores/workspaceStore.ts`, `packages/web/src/components/EditorTabs.tsx`, `packages/web/src/components/missions/`
- Ref: 0a4d7e2
- Request: Add a pinned `Team` tab to Hub connected workspace view. Add `team` to editor tab type and render `MissionPanel` for the Team tab.
- Reason: The Mission experience is Hub-only and should live beside `Activity` and `Review` as a first-class observation surface.
- Acceptance: Hub connected workspace view shows `Team` beside `Activity` and `Review`. Team tab cannot be closed. Non-Team tabs still behave as before.
- Result: Added a pinned `Team` editor tab between `Activity` and `Review`, extended editor tab metadata with `team`, and rendered `MissionPanel` from the Team tab while preserving close behavior for non-pinned tabs.
- Files: `packages/web/src/stores/workspaceStore.ts`; `packages/web/src/components/EditorTabs.tsx`; `packages/web/src/components/missions/MissionPanel.tsx`; `packages/web/src/stores/workspaceStore.test.ts`
- Verification: `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts src/stores/workspaceStore.test.ts` passed, 6 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/web build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - Team tab pinned and rendered between Activity and Review (`EditorTabs.tsx:27,145`), MissionPanel is the team-tab body, non-Team tabs retain prior close/select behavior. Hub-only entry point, matching design §4.1.
- Updated: 2026-05-06, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/stores/missionStore.ts`, web API client helpers
- Ref: 18c2b6a
- Request: Build `missionStore` and web API helpers to load Mission list, active Mission, active Mission details, refresh actions, and lifecycle activation. Keep Mission state separate from `workspaceStore.ts`.
- Reason: Team tab needs Mission state without mixing feature data into workspace tab state.
- Acceptance: Team tab can render loading, empty, active Mission, and incomplete Mission states. Refreshing Mission data does not reset editor tabs or pane state.
- Result: Added a standalone Zustand `missionStore` with Mission list/detail loading, refresh, create, activation, raw Markdown overview parsing, Kanban task count parsing, Mission Agent summary parsing, and defensive API payload normalization. `workspaceStore` only carries Team tab metadata.
- Files: `packages/web/src/stores/missionStore.ts`; `packages/web/src/stores/missionStore.test.ts`; `packages/web/src/components/missions/MissionPanel.tsx`
- Verification: `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts src/stores/workspaceStore.test.ts` passed, 6 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/web build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - Mission state lives entirely in `missionStore.ts`; `workspaceStore.ts` only carries Team tab metadata. Store exposes list/active/detail/refresh/create/activate with payload normalization. Boundary between Mission feature state and workspace state is clean.
- Updated: 2026-05-06, Squad Lead

To: Vassago | From: Squad Lead | Scope: `packages/web/src/components/missions/MissionSelector.tsx`, `packages/web/src/components/missions/MissionOverview.tsx`
- Ref: 24f9d80
- Request: Build Mission selector and compact overview. The selector should show active Mission, allow creating Mission, and allow lifecycle activation with explicit confirmation that the current active Mission will become inactive.
- Reason: The Team tab is centered around a single lifecycle active Mission and needs a compact summary above the Kanban.
- Acceptance: Selector clearly distinguishes active, inactive, completed, and incomplete Missions. Overview shows Mission name, lifecycle, task counts, and unreviewed Done count. Activating a Mission refreshes Team data.
- Result: Added `MissionSelector` with lifecycle-labelled Mission choices, refresh, new Mission creation, and explicit confirmation before activation. Added `MissionOverview` with Mission name, lifecycle, date, intent, acceptance summary, task counts, and unreviewed Done count. `MissionPanel` renders loading, empty, incomplete, and active states.
- Files: `packages/web/src/components/missions/MissionSelector.tsx`; `packages/web/src/components/missions/MissionOverview.tsx`; `packages/web/src/components/missions/MissionPanel.tsx`; `packages/web/src/stores/missionStore.ts`; `packages/web/src/stores/missionStore.test.ts`
- Verification: `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts src/stores/workspaceStore.test.ts` passed, 6 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/web build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - Selector distinguishes active/inactive/completed/incomplete with labels, activation prompts explicit confirmation that the prior active will become inactive (matches design §5.1), Overview surfaces lifecycle/task counts/unreviewed Done, and activation calls store refresh. MissionPanel covers loading/empty/active/incomplete.
- Updated: 2026-05-06, Squad Lead

To: Samigina | From: Squad Lead | Scope: `packages/web/src/components/missions/MissionKanban.tsx`, Kanban parser tests
- Ref: 3b7c1aa
- Request: Build read-only Mission Kanban parsing and rendering for `To Claim`, `In Progress`, and `Done`. Render task cards from Skill task blocks, add read-only filtering by status, Agent, and text, and add click-through to `kanban.md` source where practical.
- Reason: Kanban is the core observation surface in Team tab, but it must not mutate Mission task state.
- Acceptance: Kanban is the largest Team panel region. Team UI does not write Claim, Complete, or Review changes. Parser failures show raw fallback without corrupting files.
- Result: Added read-only Mission Kanban parsing and rendering. The parser extracts Skill task blocks from `To Claim`, `In Progress`, and `Done`, counts tasks by status and unreviewed Done state, preserves raw content on parse failure, and records source line numbers for source opening. The component renders status, Agent, and text filters with task cards and does not expose Claim, Complete, or Review mutation controls.
- Files: `packages/web/src/components/missions/MissionKanban.tsx`; `packages/web/src/components/missions/MissionPanel.tsx`; `packages/web/src/stores/missionStore.ts`; `packages/web/src/stores/missionStore.test.ts`; `packages/web/src/styles/globals.css`
- Verification: `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts` passed, 4 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/web build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - Parser handles all three sections, raw fallback preserved on parse failure with no file write, source line numbers recorded for click-through. Component exposes only status/agent/text filters and has no Claim/Complete/Review mutation paths.
- Updated: 2026-05-06, Squad Lead

To: Samigina | From: Squad Lead | Scope: `packages/web/src/components/missions/MissionAgents.tsx`, mission agents parser
- Ref: 45e0c93
- Request: Build Mission Agents observation. Parse Mission-level `agents.md` and show Agent name, responsibility, prompt summaries, and assigned task count. Do not add Agent operation buttons.
- Reason: Agent information should explain responsibilities without turning Team tab into a launch or dispatch surface.
- Acceptance: Agent area is secondary to Kanban. Agent cards have no Start Pane, Send Prompt, or Claim controls. Task counts match parsed Kanban `To` values.
- Result: Added the Mission Agents observation component and connected it into the Mission panel beside the read-only Kanban. The view shows Agent names, responsibilities, activation/initial prompt summaries, and task counts from parsed Kanban `To` values, with no Start Pane, Send Prompt, Claim, Complete, or Review controls.
- Files: `packages/web/src/components/missions/MissionAgents.tsx`; `packages/web/src/components/missions/MissionPanel.tsx`; `packages/web/src/stores/missionStore.ts`; `packages/web/src/stores/missionStore.test.ts`; `packages/web/src/styles/globals.css`
- Verification: `pnpm -C packages/web exec vitest run src/stores/missionStore.test.ts` passed, 4 tests. `pnpm -C packages/web exec tsc --noEmit` passed. `pnpm --filter @nexus/web build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - Component renders names, responsibilities, activation/initial prompt summaries, and task counts derived from parsed Kanban `To` values; no Start Pane / Send Prompt / Claim controls; raw fallback on parse failure. Agent area is rendered after Kanban in MissionPanel as designed.
- Updated: 2026-05-06, Squad Lead

To: Bael | From: Squad Lead | Scope: `.claude/skills/agent-team-mission-workflow/`, server template resolution
- Ref: a1c4e90
- Request: Treat `.claude/skills/agent-team-mission-workflow/references/` as the product Mission template source. Add server-side path resolution for those templates without duplicating templates into another runtime directory.
- Reason: Mission creation depends on a stable template source that matches the installed Skill.
- Acceptance: Server can locate `mission-workflow.md`, `agent-roster-template.md`, `mission-template.md`, `agents-template.md`, `kanban-template.md`, `roundtable-template.md`, and `squad-lead-template.md`. Missing template errors are explicit and user-readable.
- Result: Added server-side Mission Skill template resolution that reads directly from `.claude/skills/agent-team-mission-workflow/references/` and reports explicit missing-template errors.
- Files: `packages/server/src/mission/MissionService.ts`; `packages/server/src/mission/MissionService.test.ts`
- Verification: `pnpm -C packages/server test src/mission/MissionService.test.ts src/mission/routes.test.ts` passed, 9 tests. `pnpm -C packages/server test` passed, 98 tests. `pnpm --filter @nexus/server build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - `resolveMissionTemplatePaths` reads directly from the Skill references directory, all seven template filenames are covered, missing files throw user-readable errors, and no templates are duplicated into a runtime location.
- Updated: 2026-05-06, Squad Lead

To: Bael | From: Squad Lead | Scope: `packages/server/src/mission/MissionService.ts`, server tests
- Ref: c9e6f24
- Request: Build `MissionService` to discover Missions, read Mission files, create Mission files from Skill templates, write `Lifecycle: active | inactive | completed` into `mission.md`, enforce single active Mission during create/activate operations, and update `.nexus/config.yaml` `active_mission`.
- Reason: Team tab and Mission creation need a backend source for Mission lifecycle and Markdown-backed state.
- Acceptance: Creating the first Mission makes it active. Creating a later active Mission sets the previous active Mission to inactive. Creating a later inactive Mission does not change the current active Mission. Activating a Mission updates lifecycle state and `active_mission`.
- Result: Built `MissionService` discovery, create, detail read, repair, active lookup, and activation flows. Creation fills Mission files from Skill templates, writes lifecycle state into `mission.md`, preserves existing files during repair, returns raw fallback content on partial parsing, and updates `.nexus/config.yaml` `active_mission` when a Mission becomes active.
- Files: `packages/server/src/mission/MissionService.ts`; `packages/server/src/mission/MissionService.test.ts`
- Verification: `pnpm -C packages/server test src/mission/MissionService.test.ts src/mission/routes.test.ts` passed, 9 tests. `pnpm -C packages/server test` passed, 98 tests. `pnpm --filter @nexus/server build` passed.
- Review: accepted by: Squad Lead, 2026-05-06 - `createMission` enforces single-active via `deactivateOtherMissions`, first Mission auto-activates, subsequent inactive create leaves prior active untouched, `activateMission` rewrites lifecycle and updates `.nexus/config.yaml`, `repairMission` preserves existing files. Single active invariant is enforced through create/activate paths only, no separate abnormal-state model added.
- Updated: 2026-05-06, Squad Lead

To: Bael | From: Squad Lead | Scope: `packages/server/src/index.ts`, `packages/server/src/mission/*`, limited `packages/server/src/ws/handlers.ts`
- Ref: d3f8a57
- Request: Add Mission REST APIs: `GET /api/missions`, `POST /api/missions`, `GET /api/missions/active`, `POST /api/missions/:name/activate`, `GET /api/missions/:name`, and `POST /api/missions/:name/repair`.
- Reason: Hub Team tab and creation flow need structured server access to Mission data without direct client filesystem writes.
- Acceptance: APIs return structured Mission summaries and raw fallback content when parsing is partial. API writes never replace Kanban task blocks. Invalid Mission names cannot escape the workspace path.
- Result: Added Mission REST routes and wired them into the server. The routes expose list, create, active read, detail read, activate, and repair operations with structured Mission summaries, raw file fallbacks, and safe invalid-name/path-traversal rejection. No WebSocket handler changes were needed.
- Files: `packages/server/src/index.ts`; `packages/server/src/mission/routes.ts`; `packages/server/src/mission/routes.test.ts`; `packages/server/src/mission/MissionService.ts`
- Verification: `pnpm -C packages/server test src/mission/MissionService.test.ts src/mission/routes.test.ts` passed, 9 tests. `pnpm -C packages/server test` passed, 98 tests. `pnpm --filter @nexus/server build` passed. `pnpm -C packages/server exec tsc --noEmit` was attempted but is blocked by existing unrelated type errors in `src/hub/index.ts`, script declaration tests, and `src/workspace/ConfigManager.ts`.
- Review: accepted by: Squad Lead, 2026-05-06 - All six required routes are wired in `packages/server/src/index.ts:172`, `errorStatus` maps Mission name validation to 400 / not-found to 404 / duplicate to 409, `assertMissionName` blocks path traversal, raw fallbacks come through `MissionDetail.files`, and Kanban task blocks are never rewritten by API paths. Pre-existing unrelated tsc errors are out of scope.
- Updated: 2026-05-06, Squad Lead
