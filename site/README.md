# Mexus Site

Astro-based landing page for Mexus — the execution layer for multi-agent development.

```bash
cd site
pnpm install
pnpm dev      # http://localhost:4321
pnpm build    # static output in ./dist
```

Standalone from the main pnpm workspace; lives outside `packages/*` and `doc_site`.
